# 第9章 桥接模式（Bridge）

**桥接模式**将抽象部分与实现部分分离，使它们可以独立变化。它是一种结构型设计模式，通过组合而非继承来解耦抽象与实现。

## 9.1 解决的问题与应用场景

### 9.1.1 问题分析

在软件开发中，一个类可能有多个变化的维度：

```java
// 假设我们需要实现不同形状的不同颜色
// 使用继承的方式：红色圆形、蓝色圆形、红色方形、蓝色方形...
class RedCircle extends Circle { }
class BlueCircle extends Circle { }
class RedSquare extends Square { }
class BlueSquare extends Square { }

// 问题：
// 1. 类数量爆炸：每增加一种颜色或形状，都需要增加新类
// 2. 违反开闭原则：添加新颜色或形状需要修改现有类
// 3. 继承导致强耦合：子类继承父类的所有实现
```

### 9.1.2 典型应用场景

**1. 跨平台应用**

```java
// 不同操作系统上的不同文件格式
// Windows + PNG, Windows + JPG, Mac + PNG, Mac + JPG...
// 桥接：平台和文件格式独立变化
```

**2. 支付系统**

```java
// 不同支付方式 × 不同支付渠道
// 支付宝+储蓄卡、支付宝+信用卡、微信+储蓄卡、微信+信用卡...
```

**3. 消息推送**

```java
// 不同消息类型 × 不同推送渠道
// 邮件+通知、短信+通知、APP+通知、邮件+营销...
```

**4. 报表导出**

```java
// 不同格式 × 不同数据源
// PDF+MySQL、Excel+MySQL、PDF+Oracle、Excel+Oracle...
```

## 9.2 实现原理与UML

### 9.2.1 核心思想

桥接模式的核心是**将抽象部分与实现部分分离**，通过组合关系代替继承关系，使得两个维度可以独立变化。

### 9.2.2 UML类图

```
┌─────────────────────┐        ┌─────────────────────┐
│     Abstraction     │        │    Implementor     │
│    (抽象部分)        │        │    (实现部分接口)   │
├─────────────────────┤        ├─────────────────────┤
│ - implementor       │───────►│ + operationImpl()  │
│ + operation()       │        └─────────────────────┘
│ + setImplementor()  │                 ▲                │
└─────────────────────┘                 │                        │
         ┌─────────────┴─────────────┐        │                         │
         │                           │        │                         │
         ▼                           ▼
┌─────────────────────┐  ┌─────────────────────┐
│ RefinedAbstraction │  │ ConcreteImplementor│
│ (扩展抽象部分)      │  │   (具体实现)        │
├─────────────────────┤  ├─────────────────────┤
│ + operation()       │  │ + operationImpl()  │
└─────────────────────┘  └─────────────────────┘
```

### 9.2.3 角色分析

- **Abstraction（抽象部分）**：抽象类的接口，维护一个Implementor的引用
- **RefinedAbstraction（扩展抽象部分）**：扩展Abstraction的接口
- **Implementor（实现部分接口）**：定义实现类的接口
- **ConcreteImplementor（具体实现类）**：实现Implementor接口的具体类

### 9.2.4 时序图

```
Client              Abstraction           Implementor
   │                      │                       │
   │   operation()      │                       │
   │ ────────────────►  │                       │
   │                   │                       │
   │                   │   │  operationImpl()  │
   │                   │ ─────────────────────────────► │
   │                   │                       │
   │                   │   │    result         │
   │ ◄────────────────────────────────│
   │                   │                       │
```

## 9.3 代码实现

### 9.3.1 基础实现（形状和颜色）

**实现部分接口 - 颜色**

```java
public interface Color {
    String getColor();
}
```

**具体实现类 - 红色**

```java
public class Red implements Color {
    @Override
    public String getColor() {
        return "红色";
    }
}
```

**具体实现类 - 蓝色**

```java
public class Blue implements Color {
    @Override
    public String getColor() {
        return "蓝色";
    }
}
```

**抽象部分 - 形状**

```java
public abstract class Shape {
    protected Color color;

    public Shape(Color color) {
        this.color = color;
    }

    public void setColor(Color color) {
        this.color = color;
    }

    public abstract void draw();
}
```

**扩展抽象部分 - 圆形**

```java
public class Circle extends Shape {
    private int radius;

    public Circle(Color color) {
        super(color);
    }

    public Circle(Color color, int radius) {
        super(color);
        this.radius = radius;
    }

    @Override
    public void draw() {
        System.out.println("绘制" + color.getColor() + "的圆形，半径：" + radius);
    }
}
```

**扩展抽象部分 - 方形**

```java
public class Square extends Shape {
    private int side;

    public Square(Color color) {
        super(color);
    }

    public Square(Color color, int side) {
        super(color);
        this.side = side;
    }

    @Override
    public void draw() {
        System.out.println("绘制" + color.getColor() + "的方形，边长：" + side);
    }
}
```

**使用**

```java
public class Main {
    public static void main(String[] args) {
        // 红色圆形
        Shape redCircle = new Circle(new Red(), 10);
        redCircle.draw();
        // 输出: 绘制红色的圆形

        // 蓝色方形
        Shape blueSquare = new Square(new Blue(), 20);
        blueSquare.draw();
        // 输出: 绘制蓝色的方形

        // 动态改变颜色
        redCircle.setColor(new Blue());
        redCircle.draw();
        // 输出: 绘制蓝色的圆形
    }
}
```

### 9.3.2 支付系统示例

**实现部分 - 支付渠道**

```java
public interface PaymentChannel {
    void processPayment(double amount);
    void processRefund(double amount);
}
```

**具体实现 - 银行卡支付**

```java
public class BankCardPayment implements PaymentChannel {
    @Override
    public void processPayment(double amount) {
        System.out.println("通过银行卡支付：" + amount + "元");
    }

    @Override
    public void processRefund(double amount) {
        System.out.println("通过银行卡退款：" + amount + "元");
    }
}
```

**具体实现 - 第三方支付**

```java
public class ThirdPartyPayment implements PaymentChannel {
    @Override
    public void processPayment(double amount) {
        System.out.println("通过第三方支付：" + amount + "元");
    }

    @Override
    public void processRefund(double amount) {
        System.out.println("通过第三方退款：" + amount + "元");
    }
}
```

**抽象部分 - 支付方式**

```java
public abstract class PaymentMethod {
    protected PaymentChannel channel;

    public PaymentMethod(PaymentChannel channel) {
        this.channel = channel;
    }

    public void setChannel(PaymentChannel channel) {
        this.channel = channel;
    }

    public abstract void pay(double amount);
    public abstract void refund(double amount);
}
```

**扩展抽象部分 - 储蓄卡支付**

```java
public class DebitCardPayment extends PaymentMethod {
    private String cardNumber;

    public DebitCardPayment(PaymentChannel channel) {
        super(channel);
    }

    public void setCardNumber(String cardNumber) {
        this.cardNumber = cardNumber;
    }

    @Override
    public void pay(double amount) {
        // 储蓄卡特殊处理
        System.out.print("储蓄卡支付，");
        channel.processPayment(amount);
    }

    @Override
    public void refund(double amount) {
        System.out.print("储蓄卡退款，");
        channel.processRefund(amount);
    }
}
```

**扩展抽象部分 - 信用卡支付**

```java
public class CreditCardPayment extends PaymentMethod {
    private String cardNumber;
    private String cvv;

    public CreditCardPayment(PaymentChannel channel) {
        super(channel);
    }

    public void setCardInfo(String cardNumber, String cvv) {
        this.cardNumber = cardNumber;
        this.cvv = cvv;
    }

    @Override
    public void pay(double amount) {
        // 信用卡特殊处理，可能有利息
        System.out.print("信用卡支付（可能有利息），");
        channel.processPayment(amount);
    }

    @Override
    public void refund(double amount) {
        System.out.print("信用卡退款，");
        channel.processRefund(amount);
    }
}
```

**使用**

```java
public class Main {
    public static void main(String[] args) {
        PaymentChannel bankCard = new BankCardPayment();
        PaymentChannel thirdParty = new ThirdPartyPayment();

        // 储蓄卡 + 银行卡渠道
        PaymentMethod debitCard = new DebitCardPayment(bankCard);
        debitCard.pay(100);  // 储蓄卡支付，通过银行卡渠道
        // 输出: 储蓄卡支付，通过银行卡支付：100.0元

        // 信用卡 + 第三方渠道
        PaymentMethod creditCard = new CreditCardPayment(thirdParty);
        creditCard.pay(200);  // 信用卡支付，通过第三方渠道
        // 输出: 信用卡支付（可能有利息），通过第三方支付：200.0元

        // 动态切换渠道
        creditCard.setChannel(bankCard);
        creditCard.pay(200);  // 现在用银行卡渠道
        // 输出: 信用卡支付（可能有利息），通过银行卡支付：200.0元
    }
}
```

## 9.4 JDK/框架源码解析

### 9.4.1 JDBC驱动架构

JDBC是桥接模式的经典应用：

```java
// Abstraction（抽象部分）
Connection connection = DriverManager.getConnection(url, user, pwd);
Statement stmt = connection.createStatement();

// 实现部分（不同数据库驱动）
// MySQL驱动、PostgreSQL驱动、Oracle驱动等
// 通过Driver接口的具体实现连接不同数据库
```

```java
// JDBC架构
// java.sql.Connection (抽象)
// └── com.mysql.cj.jdbc.ConnectionImpl (实现)
// └── org.postgresql.jdbc1.Jdbc1Connection (实现)
// └── oracle.jdbc.OracleConnection (实现)
```

### 9.4.2 AWT中的桥接模式

Java AWT使用桥接模式分离抽象和实现：

```java
// 抽象部分：Java的Graphics类
public abstract class Graphics {
    public abstract void drawLine(int x1, int y1, int x2, int y2);
    public abstract void drawString(String str, int x, int y);
}

// 实现部分：不同平台的本地实现
public class UnixGraphics implements Graphics { /* Unix本地实现 */ }
public class WindowsGraphics implements Graphics { /* Windows本地实现 */ }
public class MacGraphics implements Graphics { /* Mac本地实现 */ }
```

### 9.4.3 SLF4J日志框架

```java
// SLF4J作为抽象层
// 底层可以绑定Log4j、Logback、JUL等实现
public static Logger logger = LoggerFactory.getLogger(Abc.class);
// LoggerFactory是抽象层
// 实际实现可以是Logback、Log4j等
```

### 9.4.4 Spring中的桥接模式

DataSource的使用：

```java
// 抽象层：DataSource
// 实现层：DriverManagerDataSource、DruidDataSource、HikariDataSource等
```

### 9.4.5 MyBatis中的桥接

Executor执行器：

```java
// 抽象层：Executor接口
// 实现层：SimpleExecutor、ReuseExecutor、BatchExecutor
```

## 9.5 使用场景与案例

### 9.5.1 消息推送系统

```java
// 实现部分 - 推送渠道
public interface PushChannel {
    void push(String userId, String title, String content);
}

// 邮件推送
public class EmailPush implements PushChannel {
    @Override
    public void push(String userId, String title, String content) {
        System.out.println("发送邮件给" + userId + ": " + title + " - " + content);
    }
}

// 短信推送
public class SmsPush implements PushChannel {
    @Override
    public void push(String userId, String title, String content) {
        System.out.println("发送短信给" + userId + ": " + content);
    }
}

// APP推送
public class AppPush implements PushChannel {
    @Override
    public void push(String userId, String title, String content) {
        System.out.println("APP推送给" + userId + ": " + title);
    }
}

// 抽象部分 - 消息类型
public abstract class Message {
    protected PushChannel channel;

    public Message(PushChannel channel) {
        this.channel = channel;
    }

    public void setChannel(PushChannel channel) {
        this.channel = channel;
    }

    public abstract void send(String userId, String title, String content);
}

// 通知消息
public class NotificationMessage extends Message {
    public NotificationMessage(PushChannel channel) {
        super(channel);
    }

    @Override
    public void send(String userId, String title, String content) {
        String formattedTitle = "[通知] " + title;
        String formattedContent = "【重要】" + content;
        channel.push(userId, formattedTitle, formattedContent);
    }
}

// 营销消息
public class MarketingMessage extends Message {
    public MarketingMessage(PushChannel channel) {
        super(channel);
    }

    @Override
    public void send(String userId, String title, String content) {
        String formattedTitle = "[促销] " + title;
        String formattedContent = content + "\n回复TD退订";
        channel.push(userId, formattedTitle, formattedContent);
    }
}

// 使用
public class Main {
    public static void main(String[] args) {
        // 通知 + 邮件
        Message notification = new NotificationMessage(new EmailPush());
        notification.send("user1", "系统更新", "系统已更新到v2.0");

        // 营销 + 短信
        Message marketing = new MarketingMessage(new SmsPush());
        marketing.send("user2", "双11促销", "全场5折起");

        // 通知 + APP
        notification.setChannel(new AppPush());
        notification.send("user3", "新消息", "您有新消息");
    }
}
```

### 9.5.2 报表导出系统

```java
// 实现部分 - 导出格式
public interface ExportFormat {
    void exportHeader(String title);
    void exportRow(List<String> row);
    void exportFooter();
}

// PDF导出
public class PdfExport implements ExportFormat {
    @Override
    public void exportHeader(String title) {
        System.out.println("PDF标题: " + title);
    }

    @Override
    public void exportRow(List<String> row) {
        System.out.println("PDF行: " + String.join(" | ", row));
    }

    @Override
    public void exportFooter() {
        System.out.println("PDF页脚");
    }
}

// Excel导出
public class ExcelExport implements ExportFormat {
    @Override
    public void exportHeader(String title) {
        System.out.println("Excel标题: " + title);
    }

    @Override
    public void exportRow(List<String> row) {
        System.out.println("Excel行: " + String.join(", ", row));
    }

    @Override
    public void exportFooter() {
        System.out.println("Excel页脚");
    }
}

// 抽象部分 - 报表生成
public abstract class ReportGenerator {
    protected ExportFormat format;

    public ReportGenerator(ExportFormat format) {
        this.format = format;
    }

    public void setFormat(ExportFormat format) {
        this.format = format;
    }

    public abstract void generate(String title, List<List<String>> data);
}

// 销售报表
public class SalesReport extends ReportGenerator {
    public SalesReport(ExportFormat format) {
        super(format);
    }

    @Override
    public void generate(String title, List<List<String>> data) {
        format.exportHeader(title + " (销售报表)");
        for (List<String> row : data) {
            format.exportRow(row);
        }
        format.exportFooter();
    }
}

// 财务报表
public class FinancialReport extends ReportGenerator {
    public FinancialReport(ExportFormat format) {
        super(format);
    }

    @Override
    public void generate(String title, List<List<String>> data) {
        format.exportHeader(title + " (财务报表)");
        for (List<String> row : data) {
            format.exportRow(row);
        }
        format.exportFooter();
    }
}

// 使用
public class Main {
    public static void main(String[] args) {
        List<List<String>> data = new ArrayList<>();
        data.add(Arrays.asList("产品A", "1000"));
        data.add(Arrays.asList("产品B", "2000"));

        ReportGenerator pdfReport = new SalesReport(new PdfExport());
        pdfReport.generate("2024年销售报告", data);

        ReportGenerator excelReport = new SalesReport(new ExcelExport());
        excelReport.generate("2024年销售报告", data);
    }
}
```

## 9.6 潜在风险与问题

### 9.6.1 设计复杂度增加

桥接模式引入了额外的抽象层，，增加了代码的复杂性。

**解决方案**：
- 确认系统确实有两个独立变化的维度
- 权衡增加的复杂度与带来的灵活性

### 9.6.2 正确识别两个维度

错误的维度划分会导致模式滥用。

**如何判断需要桥接**：
- 类有多个变化的维度
- 每个维度的变化是独立的
- 不希望使用继承（类爆炸）

### 9.6.3 与适配器模式的区别

| 特征 | 桥接模式 | 适配器模式 |
|------|----------|------------|
| 目的 | 解耦抽象与实现，使两者独立变化 | 转换接口，使不兼容变兼容 |
| 时机 | 设计时考虑 | 事后补救 |
| 结构 | 分离两个维度 | 连接已有接口 |

### 9.6.4 桥接模式的类数量

桥接模式会导致类数量增加：
- 抽象部分：N个类
- 实现部分：M个类
- 总计：N + M + 1（接口）

对比继承：
- 继承：N × M个类

## 9.7 优化策略

### 9.7.1 使用抽象工厂创建桥接

```java
public class ReportFactory {
    public static ReportGenerator createReport(String type, String format) {
        ExportFormat exportFormat;
        switch (format.toLowerCase()) {
            case "pdf":
                exportFormat = new PdfExport();
                break;
            case "excel":
                exportFormat = new ExcelExport();
                break;
            default:
                throw new IllegalArgumentException("Unknown format: " + format);
        }

        switch (type.toLowerCase()) {
            case "sales":
                return new SalesReport(exportFormat);
            case "financial":
                return new FinancialReport(exportFormat);
            default:
                throw new IllegalArgumentException("Unknown report type: " + type);
        }
    }
}
```

### 9.7.2 使用依赖注入

```java
@Component
public class ReportService {
    private final ExportFormat exportFormat;

    @Autowired
    public ReportService(ExportFormat exportFormat) {
        this.exportFormat = exportFormat;
    }

    public void generate(SalesReport report) {
        // 使用注入的实现
    }
}
```

### 9.7.3 动态切换实现

```java
// 在运行时动态切换实现
public void switchChannel(PaymentChannel newChannel) {
    this.paymentMethod.setChannel(newChannel);
}
```

### 9.7.4 最佳实践

| 场景 | 推荐 |
|------|------|
| 两个独立变化的维度 | 桥接模式 |
| 已有系统接口不兼容 | 适配器模式 |
| 需要动态切换实现 | 桥接模式 |
| 简单场景 | 直接组合 |

## 本章小结

本章详细介绍了桥接模式：

1. **解决的问题**：分离抽象与实现，使两者独立变化，避免类爆炸
2. **UML结构**：抽象部分、实现部分、扩展抽象、具体实现
3. **实现方式**：使用组合关系代替继承
4. **框架应用**：JDBC、AWT、SLF4J、Spring DataSource
5. **潜在问题**：设计复杂度增加、需