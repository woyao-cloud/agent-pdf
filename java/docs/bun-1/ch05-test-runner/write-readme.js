const fs = require('fs');
const path = 'D:/学习/大模型/pdf/java/docs/bun-1/ch05-test-runner/README.md';

// Build content using array join to avoid template literal issues
const parts = [];

parts.push('# 第五章：bun test 与 Mock 机制');
parts.push('');
parts.push('## 目录');
parts.push('');
parts.push('1. [使用场景](#1-使用场景)');
parts.push('2. [实现原理](#2-实现原理)');
parts.push('3. [潜在风险与优化](#3-潜在风险与优化)');
parts.push('4. [典型问题处理](#4-典型问题处理)');
parts.push('5. [必备知识与技能](#5-必备知识与技能)');
parts.push('6. [示例代码与配置](#6-示例代码与配置)');
parts.push('');
parts.push('---');
parts.push('');

// Section 1
parts.push('## 1. 使用场景');
parts.push('');
parts.push('### 1.1 单元测试（替代 Jest）');
parts.push('');
parts.push('Bun 内置的测试运行器是 Bun 运行时中最具吸引力的功能之一。对于绝大多数 JavaScript 和 TypeScript 项目来说，单元测试是质量保障的第一道防线。Bun 的测试运行器在设计上高度兼容 Jest 的 API，这意味着如果你之前使用 Jest 编写测试用例，迁移到 Bun 的成本极低。');
parts.push('');
parts.push('**为什么选择 Bun 替代 Jest？**');
parts.push('');
parts.push('传统的 Jest 测试框架虽然功能完善，但在大型项目中逐渐暴露出一些性能瓶颈。Jest 在运行测试时需要模拟浏览器环境，每次运行测试都需要经历完整的模块编译和转换过程。对于包含数千个测试用例的中大型项目，Jest 的全套测试运行时间可能长达数分钟甚至数十分钟。');
parts.push('');
parts.push('Bun 的测试运行器利用 Zig 语言编写的高性能运行时内核，在以下几个方面显著优于 Jest：');
parts.push('');
parts.push('第一是启动速度。Bun 测试运行器的冷启动时间通常在 10-50 毫秒级别，而 Jest 在 Node.js 上冷启动需要 200-500 毫秒。这个差异在单次运行时似乎不明显，但在 TDD 模式下，开发人员会频繁执行测试，累积的时间节省非常可观。');
parts.push('');
parts.push('第二是模块解析速度。Bun 使用自己的模块解析器，能够直接解析 TypeScript 和 JSX 源码，无需预先编译或使用额外的转换插件。在包含大量依赖的项目中，Bun 的模块解析速度比 Node.js 快 5-10 倍。');
parts.push('');
parts.push('第三是并行执行能力。Bun 测试运行器内置了多线程执行引擎，能够充分利用多核 CPU 的优势并行运行测试文件。虽然 Jest 也支持并行执行，但 Bun 的线程模型更加轻量，上下文切换开销更小。');
parts.push('');
parts.push('**Bun 测试的典型应用场景包括：**');
parts.push('');
parts.push('纯函数的单元测试。对于工具函数、验证逻辑、数据转换等不依赖外部状态的纯函数，Bun 测试运行器能够提供极快的反馈循环。开发人员可以在修改代码后的数百毫秒内获得测试结果。');
parts.push('');
parts.push('组件逻辑测试。虽然 Bun 不直接支持 DOM 渲染（这需要 happy-dom），但对于不涉及 DOM 操作的组件逻辑——比如状态管理、数据处理、自定义 Hook 的逻辑部分——Bun 测试运行器完全胜任。');
parts.push('');
parts.push('Node.js API 兼容性测试。Bun 实现了大部分 Node.js API。如果你正在编写跨运行时兼容的代码，可以使用 Bun 测试运行器验证代码在 Bun 环境中的行为。');
parts.push('');
parts.push('**从 Jest 迁移到 Bun 的注意事项：**');
parts.push('');
parts.push('Jest 的 jest.fn() 对应 Bun 的 mock()，jest.spyOn() 对应 spyOn()，jest.mock() 的模块模拟功能在 Bun 中通过 mock.module() 实现。大部分 Jest 的匹配器在 Bun 中都可用，但有一些边缘情况需要注意。');
parts.push('');
parts.push('Jest 的自定义匹配器通过 expect.extend() 扩展，Bun 同样支持该 API。Jest 的 jest.setTimeout() 在 Bun 中对应 test.setTimeout()，但 Bun 还提供了更细粒度的超时控制，允许为每个测试用例单独设置超时时间。');
parts.push('');

// 1.2
parts.push('### 1.2 API 集成测试');
parts.push('');
parts.push('Bun 测试运行器的另一个重要使用场景是 API 集成测试。由于 Bun 内置了 Bun.serve() API，你可以在测试文件的 beforeAll 钩子中启动一个轻量级的 HTTP 服务器，然后在测试用例中直接发送 HTTP 请求进行端到端测试。');
parts.push('');
parts.push('**与专用 API 测试工具的比较：**');
parts.push('');
parts.push('市面上有专门用于 API 测试的工具和库，比如 Supertest、Postman 的 Newman、SuperAgent 等。这些工具各有优势，但与 Bun 内置的测试运行器相比存在以下差异：');
parts.push('');
parts.push('Supertest 与 Express 框架深度集成，可以绕过网络层直接调用 Express 应用的内部路由处理逻辑，测试速度较快但不够真实。Bun 的 Bun.serve() 创建的是真实的 HTTP 服务器，测试覆盖了完整的网络栈，包括序列化、反序列化、HTTP 头处理等。');
parts.push('');
parts.push('Postman/Newman 提供了丰富的 GUI 界面和集合管理功能，适合非技术人员参与 API 测试。但 Newman 运行在 Node.js 环境，无法利用 Bun 的性能优势。对于技术团队来说，将 API 测试与代码仓库集成更为合理，Bun 测试运行器天然支持这种工作流。');
parts.push('');
parts.push('**API 集成测试的最佳实践：**');
parts.push('');
parts.push('在 Bun 中进行 API 集成测试时，建议遵循以下几个原则：');
parts.push('');
parts.push('服务生命周期管理。使用 beforeAll 启动服务，使用 afterAll 停止服务，确保测试之间不会因为服务状态而产生干扰。服务端口应当使用动态分配或固定端口加随机后缀的方式，避免与开发环境中的其他服务冲突。');
parts.push('');
parts.push('请求隔离。每个测试用例应当创建独立的请求，避免测试用例之间通过服务端状态产生依赖。如果测试用例需要特定的数据状态，应当在测试用例内部准备数据，而不是依赖其他测试用例的执行顺序。');
parts.push('');
parts.push('错误场景覆盖。除了测试正常的请求-响应流程外，还应当覆盖各种错误场景，包括 404 未找到、400 参数错误、500 服务器错误、超时、连接断开等。');
parts.push('');
parts.push('性能测试。虽然 Bun 测试运行器不是专门的性能测试工具，但你可以利用 Bun.nanoseconds() 等计时 API 来测量 API 端点的响应时间。');
parts.push('');

// 1.3
parts.push('### 1.3 快照测试');
parts.push('');
parts.push('快照测试（Snapshot Testing）是 Jest 生态中广受欢迎的功能，Bun 测试运行器也提供了支持。快照测试的核心思想是将组件或函数的输出序列化为字符串，并与之前保存的"快照"进行比较。如果输出发生变化，测试失败并提示开发人员检查变更是否有意。');
parts.push('');
parts.push('**Bun 快照测试的工作流程：**');
parts.push('');
parts.push('首次运行包含快照匹配的测试时，Bun 会创建 .snap 文件并将序列化的输出写入其中。后续运行时，Bun 会将实际输出与 .snap 文件中的内容进行比较。如果匹配，测试通过；如果不匹配，测试失败并显示差异。');
parts.push('');
parts.push('Bun 使用 toMatchSnapshot() 匹配器来创建和验证快照。与 Jest 不同的是，Bun 的快照文件格式经过优化，更加紧凑且易于人工阅读。Bun 快照文件是标准的 JSON 格式，而不是 Jest 使用的自定义格式。');
parts.push('');
parts.push('**快照测试的适用场景：**');
parts.push('');
parts.push('组件渲染输出。对于 UI 组件，快照测试可以捕获渲染结果的完整结构，确保组件的输出不会意外变化。Bun 与 happy-dom 配合使用时，可以将组件的 DOM 输出序列化为快照。');
parts.push('');
parts.push('序列化数据结构。对于 API 响应、配置对象、数据库查询结果等复杂数据结构，手动编写断言非常繁琐且容易遗漏字段。快照测试可以自动捕获完整的数据结构，并在数据结构发生变化时及时通知开发人员。');
parts.push('');
parts.push('错误消息和日志格式。对于需要保持稳定的错误消息格式，快照测试是一种有效的回归防护手段。');
parts.push('');
parts.push('**快照测试的注意事项：**');
parts.push('');
parts.push('快照文件应当纳入版本控制。.snap 文件是测试的预期结果，团队成员应当共享这些文件以确保测试的一致性。在代码审查时，审查者应当检查快照文件的变更是否合理。');
parts.push('');
parts.push('快照更新使用 --update-snapshots 标志（或简写 -u）。当开发人员确认输出变更有意时，可以使用该标志更新快照文件。更新后，建议立即运行完整的测试套件，确认没有其他测试因为快照更新而失败。');
parts.push('');
parts.push('快照测试不是单元测试的替代品。快照测试只能告诉你输出发生了变化，不能告诉你变化是否正确。因此，快照测试应当与传统的断言式测试结合使用，而不是完全依赖快照。');
parts.push('');

// 1.4
parts.push('### 1.4 DOM 测试与 happy-dom');
parts.push('');
parts.push('对于涉及 DOM 操作的代码，Bun 推荐使用 happy-dom 而不是 jsdom。happy-dom 是一个用 TypeScript 编写的轻量级 DOM 实现，其 API 兼容 Web 标准，但实现更加简洁高效。');
parts.push('');
parts.push('**为什么选择 happy-dom 而非 jsdom？**');
parts.push('');
parts.push('jsdom 是 Node.js 生态中最流行的 DOM 实现，但它有几个显著的问题。jsdom 使用 JavaScript 实现，解析速度较慢。jsdom 的 API 实现存在许多与浏览器行为不一致的地方，经常导致测试结果与生产环境不符。jsdom 的内存占用较高，在大型测试套件中可能成为性能瓶颈。');
parts.push('');
parts.push('happy-dom 在设计上更加轻量。它的核心实现只有几千行代码，解析速度快，内存占用低。happy-dom 的 API 更加贴近现代 Web 标准，对于大多数常见 DOM 操作，happy-dom 的表现与浏览器一致。');
parts.push('');
parts.push('**在 Bun 中使用 happy-dom：**');
parts.push('');
parts.push('在 Bun 项目中配置 happy-dom 非常简单。你只需要在测试文件中引入 happy-dom 的全局设置，或者使用 Bun 的配置文件进行全局配置。');
parts.push('');
parts.push('注册后，document、window、navigator 等全局对象即可在测试环境中使用。你可以像在浏览器中一样创建 DOM 元素、监听事件、操作样式。');
parts.push('');
parts.push('**DOM 测试的典型模式：**');
parts.push('');
parts.push('DOM 测试通常遵循 Arrange-Act-Assert 模式。首先准备 DOM 结构，然后执行操作，最后验证结果。Bun 测试运行器的 expect API 与 DOM 操作配合良好。');
parts.push('');
parts.push('对于更复杂的 DOM 测试场景，比如事件处理、动画、异步渲染等，happy-dom 提供了标准 API 的支持。Bun 测试运行器的异步测试能力与这些 API 结合，可以实现对交互行为的完整测试覆盖。');
parts.push('');

// Section 2
parts.push('---');
parts.push('');
parts.push('## 2. 实现原理');
parts.push('');
parts.push('### 2.1 bun:test 模块内部架构');
parts.push('');
parts.push('bun:test 是 Bun 运行时内置的测试模块，与普通的 npm 包不同，它直接编译在 Bun 的二进制文件中。这意味着你不需要安装任何额外的依赖就可以使用测试功能。');
parts.push('');
parts.push('**bun:test 的架构层次：**');
parts.push('');
parts.push('bun:test 的架构可以分为三个层次：');
parts.push('');
parts.push('底层是 Bun 运行时内核。这一层由 Zig 语言编写，负责测试文件的解析、模块加载、类型检查等基础功能。Bun 的模块解析器能够直接处理 TypeScript、JSX、TSX 文件，无需预先编译。');
parts.push('');
parts.push('中间层是测试运行器引擎。这一层负责测试用例的调度和执行。Bun 的测试运行器引擎实现了 describe-it 模式的完整语义，包括嵌套描述块、异步测试支持、超时控制、生命周期钩子等。引擎使用工作线程池来实现并行执行。');
parts.push('');
parts.push('上层是断言库和匹配器。这一层提供了 expect 函数及其配套的匹配器方法。Bun 的断言库实现了 Jest 兼容的 API，包括 toBe、toEqual、toStrictEqual、toMatch、toContain、toThrow 等常用匹配器。');
parts.push('');
parts.push('**测试执行的生命周期：**');
parts.push('');
parts.push('当你在终端中运行 bun test 时，Bun 会经历以下步骤：');
parts.push('');
parts.push('第一步，文件发现。Bun 扫描当前目录及其子目录，查找匹配测试文件模式的文件。默认模式包括 *.test.{js|ts|jsx|tsx}、*.spec.{js|ts|jsx|tsx}，以及 __tests__ 目录下的所有文件。');
parts.push('');
parts.push('第二步，解析和编译。Bun 使用其内置的模块解析器加载每个测试文件及其依赖。对于 TypeScript 文件，Bun 使用内置的 TypeScript 解析器进行类型擦除，提取 JavaScript 代码。这个过程不需要 tsc 或 babel，速度极快。');
parts.push('');
parts.push('第三步，测试注册。Bun 执行测试文件的顶层代码，注册 describe 块和 it 测试用例。在注册阶段，Bun 构建测试用例的层次结构树，记录每个测试用例的名称、位置、超时设置和依赖关系。');
parts.push('');
parts.push('第四步，测试执行。Bun 根据配置决定是串行还是并行执行测试文件。对于并行执行，Bun 创建工作线程池，每个工作线程加载一个测试文件并执行其中的测试用例。');
parts.push('');
parts.push('第五步，结果报告。Bun 汇总所有测试文件的结果，生成格式化的测试报告。默认的报告格式包括测试用例的数量、通过率、失败详情和执行时间。Bun 还支持 JUnit XML 格式的输出，便于与 CI/CD 系统集成。');
parts.push('');

// 2.2
parts.push('### 2.2 Jest 兼容层');
parts.push('');
parts.push('Bun 测试运行器在设计目标中明确包含了"与 Jest 兼容"这一要求。为了实现这个目标，Bun 实现了一个 Jest 兼容层，将 Jest 的 API 映射到 Bun 的内部实现上。');
parts.push('');
parts.push('**兼容层的实现策略：**');
parts.push('');
parts.push('Bun 的 Jest 兼容层采用了适配器模式。对于每个 Jest API，Bun 检查其语义和参数签名，然后实现一个行为一致的版本。');
parts.push('');
parts.push('jest.fn() 直接映射到 mock()。两者的底层实现都是创建一个可追踪调用信息的函数包装器。Bun 的 mock() 函数返回一个 MockFunction 对象，该对象实现了与 Jest 相同的接口。');
parts.push('');
parts.push('jest.spyOn() 映射到 spyOn()。两者的实现都是在目标对象的方法上创建一个拦截器，记录方法调用信息，同时保留原始方法的默认行为。');
parts.push('');
parts.push('jest.mock() 映射到 mock.module()。模块模拟是最复杂的兼容性挑战。Jest 在模块加载之前注入模拟实现，而 Bun 在模块解析阶段拦截请求。虽然行为类似，但实现机制不同，这导致了一些边缘情况下的行为差异。');
parts.push('');
parts.push('**兼容性矩阵：**');
parts.push('');
parts.push('| API 类别 | Jest API | Bun API | 兼容性 |');
parts.push('|---------|---------|---------|-------|');
parts.push('| 测试结构 | describe | describe | 完全兼容 |');
parts.push('| 测试用例 | it/test | it/test | 完全兼容 |');
parts.push('| 断言 | expect | expect | 完全兼容 |');
parts.push('| 匹配器 | toBe | toBe | 完全兼容 |');
parts.push('| 匹配器 | toEqual | toEqual | 完全兼容 |');
parts.push('| 匹配器 | toStrictEqual | toStrictEqual | 完全兼容 |');
parts.push('| 匹配器 | toMatchSnapshot | toMatchSnapshot | 兼容但格式不同 |');
parts.push('| 模拟函数 | jest.fn | mock | 完全兼容 |');
parts.push('| 方法监视 | jest.spyOn | spyOn | 完全兼容 |');
parts.push('| 模块模拟 | jest.mock | mock.module | 基本兼容 |');
parts.push('| 定时器模拟 | jest.useFakeTimers | 不支持 | 需自行实现 |');
parts.push('| 自定义匹配器 | expect.extend | expect.extend | 完全兼容 |');
parts.push('| 生命周期 | beforeAll | beforeAll | 完全兼容 |');
parts.push('| 生命周期 | afterAll | afterAll | 完全兼容 |');
parts.push('| 生命周期 | beforeEach | beforeEach | 完全兼容 |');
parts.push('| 生命周期 | afterEach | afterEach | 完全兼容 |');
parts.push('| 测试过滤 | test.only | test.only | 完全兼容 |');
parts.push('| 测试跳过 | test.skip | test.skip | 完全兼容 |');
parts.push('| 超时设置 | jest.setTimeout | test.setTimeout | 兼容但 API 不同 |');
parts.push('');

// 2.3
parts.push('### 2.3 Mock 函数拦截机制');
parts.push('');
parts.push('Mock 函数是测试中最重要的工具之一。Bun 的 mock 函数实现基于 JavaScript 的 Proxy 和闭包特性，提供了一种轻量级的函数拦截机制。');
parts.push('');
parts.push('**Mock 函数的核心实现：**');
parts.push('');
parts.push('在 Bun 的内部实现中，mock() 函数创建一个包装函数，该函数在每次调用时执行以下操作：');
parts.push('');
parts.push('记录调用参数。包装函数将每次调用的参数列表保存在 mock.calls 数组中。每个条目是一个参数数组，包含了该次调用的所有参数。');
parts.push('');
parts.push('记录调用上下文。包装函数记录每次调用时的 this 值，保存在 mock.instances 数组中。这对于测试对象方法的行为很重要。');
parts.push('');
parts.push('记录调用结果。包装函数记录每次调用的返回值或抛出的异常，保存在 mock.results 数组中。');
parts.push('');
parts.push('追踪调用次数。包装函数维护一个计数器，记录函数被调用的总次数。这个计数器用于 toHaveBeenCalledTimes() 匹配器的验证。');
parts.push('');

// 2.4
parts.push('### 2.4 模块模拟的实现');
parts.push('');
parts.push('模块模拟（Module Mocking）是测试中最强大的功能之一。它允许你在测试环境中替换整个模块的实现，从而隔离被测试的代码。');
parts.push('');
parts.push('**Bun 的模块模拟实现：**');
parts.push('');
parts.push('Bun 的 mock.module() 与 Jest 的 jest.mock() 在实现机制上有本质区别。Jest 在模块加载之前注入模拟实现，而 Bun 在模块解析阶段拦截请求。');
parts.push('');
parts.push('当你在测试文件中调用 mock.module() 时，Bun 会在其模块注册表中注册一个模拟工厂函数。后续当测试代码导入该模块时，Bun 的模块解析器会检查注册表，如果发现匹配的模拟注册，则调用工厂函数生成模拟模块。');
parts.push('');
parts.push('这种实现方式有几个优点：模拟在模块解析层面生效，无论模块如何被导入，模拟都会生效。模拟对模块的所有导入者生效，不需要显式的模拟调用顺序。');
parts.push('');
parts.push('但也有一些限制：模拟必须在模块首次导入之前注册。如果模块已经被加载，后续的 mock.module() 调用不会生效。模拟的作用域是文件级别的，不同测试文件之间的模拟不会互相影响。');
parts.push('');

// 2.5
parts.push('### 2.5 快照比较算法');
parts.push('');
parts.push('快照测试的核心是比较算法。Bun 的快照比较算法在设计和实现上与 Jest 有显著差异。');
parts.push('');
parts.push('**Bun 快照的序列化格式：**');
parts.push('');
parts.push('Bun 使用 JSON 格式存储快照，每个快照条目是一个 JSON 字符串。与 Jest 使用的自定义格式相比，JSON 格式有以下优势：');
parts.push('');
parts.push('标准化。JSON 是广泛接受的标准化格式，任何工具都可以解析和生成。');
parts.push('');
parts.push('可读性。JSON 格式具有清晰的结构层次，对于复杂数据结构的快照，JSON 的可读性优于 Jest 的自定义格式。');
parts.push('');
parts.push('兼容性。JSON 格式使得快照文件可以与其他工具集成。');
parts.push('');
parts.push('**快照比较算法：**');
parts.push('');
parts.push('Bun 的快照比较算法执行以下步骤：序列化、加载、比较、更新。');
parts.push('');
parts.push('第一步，序列化。Bun 将被测试的值转换为 JSON 字符串。第二步，加载。Bun 从 .snap 文件中加载对应的快照条目。第三步，比较。Bun 使用字符串比较算法比较实际值和快照值。第四步，更新。如果指定了 --update-snapshots 标志，Bun 将实际值写入 .snap 文件。');
parts.push('');

// Section 3
parts.push('---');
parts.push('');
parts.push('## 3. 潜在风险与优化');
parts.push('');
parts.push('### 3.1 Jest API 覆盖缺口');
parts.push('');
parts.push('虽然 Bun 测试运行器在 API 层面上高度兼容 Jest，但仍然存在一些缺口和差异。了解这些差异对于成功从 Jest 迁移到 Bun 至关重要。');
parts.push('');
parts.push('**缺失的 Jest API：**');
parts.push('');
parts.push('定时器模拟（Fake Timers）。Jest 的 jest.useFakeTimers() 和 jest.useRealTimers() 在 Bun 中尚未实现。这意味着如果你使用 setTimeout、setInterval、setImmediate 等定时器函数进行测试，在 Bun 中需要等待实际的定时器触发。');
parts.push('');
parts.push('jest.isolateModules()。Jest 的这个 API 允许在隔离的模块环境中测试模块加载行为。Bun 目前没有对应的实现。');
parts.push('');
parts.push('jest.requireActual()。在模拟模块时，Jest 允许使用 jest.requireActual() 获取模块的真实实现。Bun 的 mock.module() 不支持类似的 API。');
parts.push('');
parts.push('jest.createMockFromModule()。Jest 的这个 API 可以自动从模块创建模拟实现。Bun 没有提供对应的功能，你需要手动编写模拟实现。');
parts.push('');
parts.push('**行为差异：**');
parts.push('');
parts.push('toMatchSnapshot() 的快照格式不同。虽然 Bun 和 Jest 都支持快照测试，但生成的快照文件格式不兼容。如果你从 Jest 迁移到 Bun，需要重新生成所有快照文件。');
parts.push('');
parts.push('jest.mock() 的模块模拟在 Bun 中使用 mock.module()，两者的 API 参数签名有所不同。');
parts.push('');
parts.push('**兼容性建议：**');
parts.push('');
parts.push('如果你计划从 Jest 迁移到 Bun，建议采取渐进式迁移策略。首先在 Bun 中运行简单的单元测试，验证基本功能正常。然后逐步迁移更复杂的测试。');
parts.push('');

// 3.2
parts.push('### 3.2 快照格式差异');
parts.push('');
parts.push('Bun 和 Jest 的快照格式差异是迁移过程中最容易被忽视的问题之一。');
parts.push('');
parts.push('**格式差异的具体表现：**');
parts.push('');
parts.push('文件结构。Jest 的快照文件使用自定义的格式，每个快照条目以 exports[ 测试名称 ] 开始。Bun 的快照文件使用标准的 JSON 格式，每个快照条目以测试名称为键，序列化的值为值。');
parts.push('');
parts.push('序列化方式。Jest 使用 pretty-format 库进行序列化，输出的格式可读性较好，但比较冗长。Bun 使用 JSON 序列化，输出的格式更加紧凑。');
parts.push('');
parts.push('**快照迁移策略：**');
parts.push('');
parts.push('如果从 Jest 迁移到 Bun，快照文件需要重新生成。推荐的迁移步骤：在 Jest 中运行所有测试确保快照最新，切换到 Bun 运行测试，使用 bun test --update-snapshots 重新生成所有快照，审查快照文件的变更。');
parts.push('');
parts.push('**快照维护最佳实践：**');
parts.push('');
parts.push('快照文件应当小而专注。每个快照文件对应一个测试文件。快照更新应当谨慎。在更新快照之前，确保输出变更确实是有意的。避免使用快照测试不稳定的输出。');
parts.push('');

// 3.3
parts.push('### 3.3 DOM 测试使用 happy-dom 而非 jsdom');
parts.push('');
parts.push('Bun 推荐使用 happy-dom 进行 DOM 测试，而不是 Node.js 生态中更常见的 jsdom。这个选择对测试策略有重要影响。');
parts.push('');
parts.push('**happy-dom 与 jsdom 的对比：**');
parts.push('');
parts.push('| 特性 | happy-dom | jsdom |');
parts.push('|------|----------|-------|');
parts.push('| 实现语言 | TypeScript | JavaScript |');
parts.push('| 包大小 | ~500KB | ~10MB |');
parts.push('| 启动时间 | <10ms | 50-200ms |');
parts.push('| API 兼容性 | 现代 Web 标准 | 传统 Web 标准 |');
parts.push('| CSS 支持 | 基础支持 | 较好支持 |');
parts.push('| 维护状态 | 活跃 | 维护模式 |');
parts.push('| Bun 集成 | 官方推荐 | 需要额外配置 |');
parts.push('');
parts.push('**happy-dom 的限制：**');
parts.push('');
parts.push('虽然 happy-dom 在大多数场景下表现良好，但它有一些限制需要注意：CSS 解析和计算有限、不支持 Canvas API 和 WebGL、不执行布局计算。');
parts.push('');

// 3.4
parts.push('### 3.4 大型测试套件的性能优化');
parts.push('');
parts.push('随着项目规模的扩大，测试套件的执行时间会逐渐增长。Bun 虽然比 Jest 快很多，但大型测试套件仍然需要优化。');
parts.push('');
parts.push('**性能瓶颈分析：**');
parts.push('');
parts.push('大型测试套件的性能瓶颈通常出现在以下几个方面：文件 I/O、模块解析、并行执行开销、内存使用。');
parts.push('');
parts.push('**优化策略：**');
parts.push('');
parts.push('合理分组。将测试文件按功能模块分组，每个组可以独立运行。在开发阶段，只运行修改的模块对应的测试文件。');
parts.push('');
parts.push('使用 --bail 标志。该标志在遇到第一个失败的测试时停止运行。');
parts.push('');
parts.push('配置超时时间。为每个测试用例设置合适的超时时间，避免长时间运行的测试拖慢整体速度。');
parts.push('');
parts.push('使用测试选择器。Bun 支持通过名称模式选择要运行的测试。');
parts.push('');
parts.push('减少测试间的依赖。测试用例之间应当互相独立，不共享状态。');
parts.push('');
parts.push('**CI/CD 环境优化：**');
parts.push('');
parts.push('在 CI/CD 环境中运行 Bun 测试时，还需要考虑：使用缓存、限制并行度、选择合适的报告格式。');
parts.push('');

// Section 4
parts.push('---');
parts.push('');
parts.push('## 4. 典型问题处理');
parts.push('');
parts.push('### 4.1 expect API 不工作');
parts.push('');
parts.push('**问题描述：**');
parts.push('');
parts.push('在 Bun 测试中，有时会出现 expect API 不工作的情况。这通常表现为 expect is not defined 错误，或者某些匹配器方法不存在。');
parts.push('');
parts.push('**原因分析：**');
parts.push('');
parts.push('expect 未定义的最常见原因是导入路径错误。expect 必须从 bun:test 模块导入，而不是从其他模块。');
parts.push('');
parts.push('另一种可能的原因是在测试文件中混用了不同的测试框架。');
parts.push('');
parts.push('**解决方案：**');
parts.push('');
parts.push('确保所有测试 API 都从 bun:test 导入。如果某个匹配器方法不存在，检查 Bun 的版本。');
parts.push('');

// 4.2
parts.push('### 4.2 Mock 不工作');
parts.push('');
parts.push('**问题描述：**');
parts.push('');
parts.push('Mock 函数有时不按预期工作，表现为模拟函数未被调用、调用参数记录不正确、或者模块模拟未生效。');
parts.push('');
parts.push('**原因分析：**');
parts.push('');
parts.push('模块模拟不生效的最常见原因是模拟注册发生在模块首次导入之后。Bun 的 mock.module() 要求在模块首次导入之前注册模拟。');
parts.push('');
parts.push('**解决方案：**');
parts.push('');
parts.push('确保模拟注册在模块导入之前。Bun 支持在测试文件顶部使用模拟注册，然后在 describe/it 块中使用动态导入。');
parts.push('');
parts.push('对于 mock 函数调用计数不准确的问题，确保在每个测试用例开始前重置 mock 状态。');
parts.push('');

// 4.3
parts.push('### 4.3 快照更新');
parts.push('');
parts.push('**问题描述：**');
parts.push('');
parts.push('快照测试失败，需要更新快照文件。当代码的预期输出发生变化时，旧的快照文件不再匹配。');
parts.push('');
parts.push('**原因分析：**');
parts.push('');
parts.push('快照失败的原因可能包括：功能变更、非确定性输出、环境差异。');
parts.push('');
parts.push('**解决方案：**');
parts.push('');
parts.push('当快照测试失败时，首先确认输出变更是否是有意的。如果变更有意，使用 bun test --update-snapshots 更新快照文件。');
parts.push('');
parts.push('**预防非确定性快照：**');
parts.push('');
parts.push('为了避免非确定性值导致快照测试失败，建议在测试中对数据进行预处理，移除或替换不稳定字段。');
parts.push('');

// 4.4
parts.push('### 4.4 测试超时');
parts.push('');
parts.push('**问题描述：**');
parts.push('');
parts.push('测试用例执行超时，Bun 报告超时错误。');
parts.push('');
parts.push('**原因分析：**');
parts.push('');
parts.push('测试超时的常见原因包括：异步操作未正确完成、未捕获的异常、资源竞争、死循环或无限递归。');
parts.push('');
parts.push('**解决方案：**');
parts.push('');
parts.push('首先，检查异步操作是否正确处理了所有路径。其次，为长时间运行的测试设置合适的超时时间。第三，检查测试是否存在资源泄漏。');
parts.push('');

// 4.5
parts.push('### 4.5 常见错误速查表');
parts.push('');
parts.push('| 错误信息 | 可能原因 | 解决方案 |');
parts.push('|---------|---------|---------|');
parts.push('| expect is not defined | 未从 bun:test 导入 | 添加 import |');
parts.push('| mock is not defined | 未从 bun:test 导入 | 添加 import |');
parts.push('| spyOn is not a function | 版本过旧 | 升级 Bun |');
parts.push('| Cannot find module | 导入路径错误 | 检查路径 |');
parts.push('| Test timed out | 异步操作未完成 | 增加超时 |');
parts.push('| Snapshot does not match | 输出变化 | 使用 -u 更新 |');
parts.push('| mock.module() not working | 模块已导入 | 确保先注册 |');
parts.push('');

// Section 5
parts.push('---');
parts.push('');
parts.push('## 5. 必备知识与技能');
parts.push('');
parts.push('### 5.1 测试金字塔理论');
parts.push('');
parts.push('测试金字塔是 Mike Cohn 在《Succeeding with Agile》一书中提出的概念，用于描述不同层次测试的推荐比例。');
parts.push('');
parts.push('**测试金字塔的结构：**');
parts.push('');
parts.push('测试金字塔从下到上包含三个层次：');
parts.push('');
parts.push('底层是单元测试（Unit Tests）。单元测试测试最小的代码单元——通常是单个函数或方法。单元测试的特点是：执行速度快，通常在毫秒级别；隔离性好，不依赖外部资源；维护成本低。在测试金字塔中，单元测试应该占据最大比例，通常达到 70% 以上。');
parts.push('');
parts.push('中间层是集成测试（Integration Tests）。集成测试测试多个组件之间的交互。集成测试的特点是：执行速度中等，通常在秒级别；涉及组件间的数据流和通信。集成测试应该占据中等比例，通常在 20% 左右。');
parts.push('');
parts.push('顶层是端到端测试（E2E Tests）。端到端测试测试完整的系统流程。端到端测试的特点是：执行速度慢，通常在分钟级别；模拟真实用户操作。端到端测试应该占据最小比例，通常在 10% 以下。');
parts.push('');
parts.push('**Bun 测试在测试金字塔中的位置：**');
parts.push('');
parts.push('Bun 测试运行器主要覆盖测试金字塔的底层和中层。对于单元测试，Bun 提供了快速的执行速度和丰富的断言 API。对于集成测试，Bun 的 Bun.serve() 和 fetch API 可以方便地搭建测试服务器和发送请求。');
parts.push('');

// 5.2
parts.push('### 5.2 Mock/Stub/Spy 的区别');
parts.push('');
parts.push('在测试中，Mock、Stub 和 Spy 是三种不同的测试替身（Test Double）模式。理解它们的区别对于编写高质量的测试至关重要。');
parts.push('');
parts.push('**Stub（桩）：**');
parts.push('');
parts.push('Stub 是提供预设返回值的对象或函数。Stub 的主要目的是控制测试环境，确保被测试代码能够获得预期的输入。Stub 的特点：只返回预设值，不记录调用信息。');
parts.push('');
parts.push('**Mock（模拟）：**');
parts.push('');
parts.push('Mock 是可编程期望行为的对象或函数。Mock 不仅提供预设返回值，还验证调用方式是否符合预期。Mock 的特点：记录调用信息，验证行为是否符合预期。');
parts.push('');
parts.push('**Spy（间谍）：**');
parts.push('');
parts.push('Spy 是包装真实对象的观察者。Spy 在真实行为的基础上添加记录功能，既不改变原始行为，又能观察调用情况。Spy 的特点：保留原始行为，记录调用信息。');
parts.push('');
parts.push('**三者的选择指南：**');
parts.push('');
parts.push('| 场景 | 推荐使用 | 原因 |');
parts.push('|------|---------|------|');
parts.push('| 需要预设返回值 | Stub | 最简单，开销最小 |');
parts.push('| 需要验证调用 | Mock | 提供完整的调用记录 |');
parts.push('| 需要保留原始行为 | Spy | 在真实行为上添加观察 |');
parts.push('| 替代外部依赖 | Stub | 提供可控的输入 |');
parts.push('| 验证交互正确性 | Mock | 验证调用参数和次数 |');
parts.push('| 测试现有代码 | Spy | 不改变现有行为 |');
parts.push('');

// 5.3
parts.push('### 5.3 TDD 方法论');
parts.push('');
parts.push('测试驱动开发（Test-Driven Development，TDD）是一种软件开发方法论，强调在编写实现代码之前先编写测试。');
parts.push('');
parts.push('**TDD 的核心循环：**');
parts.push('');
parts.push('TDD 遵循"红-绿-重构"循环：');
parts.push('');
parts.push('红（Red）：编写一个失败的测试。在测试中描述期望的行为，然后运行测试，确认测试失败。这个步骤确保测试本身是有效的。');
parts.push('');
parts.push('绿（Green）：编写最简单的实现代码使测试通过。不要追求完美的实现，只需要让测试通过。');
parts.push('');
parts.push('重构（Refactor）：在测试保护下重构代码。改进代码质量，消除重复，优化性能。');
parts.push('');
parts.push('**TDD 的优势：**');
parts.push('');
parts.push('TDD 的核心价值在于设计，而非测试本身。编写测试迫使我们从调用者的角度思考 API 设计。TDD 产生的代码通常具有更好的模块化程度和更低的耦合度。');
parts.push('');
parts.push('**TDD 的常见陷阱：**');
parts.push('');
parts.push('测试过于复杂。如果一个测试需要大量设置代码，说明被测试的代码可能设计不够好。测试与实现耦合过紧。跳过重构步骤。');
parts.push('');

// 5.4
parts.push('### 5.4 代码覆盖率指标');
parts.push('');
parts.push('代码覆盖率是衡量测试套件质量的指标之一。Bun 测试运行器支持代码覆盖率收集，可以帮助你了解测试覆盖了哪些代码。');
parts.push('');
parts.push('**覆盖率类型：**');
parts.push('');
parts.push('行覆盖率（Line Coverage）。衡量测试执行了多少行代码。这是最常见的覆盖率指标。');
parts.push('');
parts.push('分支覆盖率（Branch Coverage）。衡量测试覆盖了多少代码分支。对于 if-else、switch 等条件语句，分支覆盖率比行覆盖率更有意义。');
parts.push('');
parts.push('函数覆盖率（Function Coverage）。衡量测试调用了多少函数。');
parts.push('');
parts.push('语句覆盖率（Statement Coverage）。衡量测试执行了多少语句。');
parts.push('');
parts.push('**在 Bun 中收集覆盖率：**');
parts.push('');
parts.push('Bun 内置了覆盖率收集功能，使用 --coverage 标志即可启用。Bun 可以生成文本格式和 HTML 格式的覆盖率报告。');
parts.push('');
parts.push('**覆盖率指标的使用建议：**');
parts.push('');
parts.push('覆盖率不是目标，而是工具。追求 100% 覆盖率不一定有意义。关注未覆盖的代码。分支覆盖率优先。结合代码审查。');
parts.push('');

// Section 6
parts.push('---');
parts.push('');
parts.push('## 6. 示例代码与配置');
parts.push('');
parts.push('### 6.1 math.test.ts 逐行讲解');
parts.push('');
parts.push('math.test.ts 是 Bun 测试的基础示例，展示了 Bun 测试运行器的核心功能。');
parts.push('');
parts.push('**导入语句：**');
parts.push('');
parts.push('import { describe, it, expect } from "bun:test" 从 bun:test 模块导入了三个最常用的 API。describe 用于创建测试分组，it 用于定义测试用例，expect 用于编写断言。bun:test 是 Bun 内置模块，不需要安装任何 npm 包。');
parts.push('');
parts.push('**测试分组：**');
parts.push('');
parts.push('describe 创建一个测试分组，第一个参数是分组的名称，第二个参数是一个回调函数，包含该分组下的所有测试用例。describe 块可以嵌套，形成树形的测试结构。');
parts.push('');
parts.push('**基础断言：**');
parts.push('');
parts.push('it("should add two numbers", () => { expect(1 + 1).toBe(2); }) 验证了最基本的数学运算。expect 返回一个 Expect 对象，toBe 是匹配器方法，验证预期值和实际值是否严格相等。');
parts.push('');
parts.push('**异步测试：**');
parts.push('');
parts.push('Bun 测试运行器原生支持异步测试。只要测试函数返回 Promise，Bun 会自动等待 Promise 完成。');
parts.push('');
parts.push('**对象比较：**');
parts.push('');
parts.push('toEqual 是对象比较的匹配器。与 toBe 不同，toEqual 执行深度比较，递归地比较对象的每个属性。');
parts.push('');
parts.push('**类型检查：**');
parts.push('');
parts.push('使用 JavaScript 的内置类型检查函数结合 Bun 的测试 API 进行类型检查和布尔断言。');
parts.push('');

// 6.2
parts.push('### 6.2 mock.test.ts 逐行讲解');
parts.push('');
parts.push('mock.test.ts 展示了 Bun 的 Mock 和 Spy 功能。');
parts.push('');
parts.push('**导入 Mock 相关 API：**');
parts.push('');
parts.push('除了 describe、it、expect 之外，这个文件还导入了 mock 和 spyOn。mock 用于创建模拟函数，spyOn 用于监视对象的方法。');
parts.push('');
parts.push('**测试准备：**');
parts.push('');
parts.push('定义一个 calculator 对象，用于演示 spyOn 的使用。');
parts.push('');
parts.push('**创建基本 Mock：**');
parts.push('');
parts.push('mock(() => 42) 创建一个模拟函数，当调用时返回 42。expect(fn).toHaveBeenCalledTimes(1) 验证模拟函数被调用了一次。');
parts.push('');
parts.push('**监视对象方法：**');
parts.push('');
parts.push('spyOn(calculator, "add") 创建一个间谍，监视 calculator.add 方法的调用。与 mock 不同，spyOn 保留原始方法的默认行为。spy.mockRestore() 将方法恢复为原始方法。');
parts.push('');
parts.push('**模块模拟：**');
parts.push('');
parts.push('展示如何创建模拟的 fetch 函数。在实际项目中，可以使用 mock.module 替换整个模块。');
parts.push('');

// 6.3
parts.push('### 6.3 api.test.ts 逐行讲解');
parts.push('');
parts.push('api.test.ts 展示了如何使用 Bun 进行 API 集成测试。');
parts.push('');
parts.push('**导入生命周期 API：**');
parts.push('');
parts.push('导入 beforeAll 和 afterAll 生命周期钩子，用于管理测试环境的生命周期。');
parts.push('');
parts.push('**启动和关闭服务器：**');
parts.push('');
parts.push('beforeAll 中使用 Bun.serve() 创建 HTTP 服务器，afterAll 中关闭服务器。Bun.serve() 是 Bun 内置的 HTTP 服务器 API，比 Node.js 的 http.createServer 更简洁。');
parts.push('');
parts.push('**健康检查测试：**');
parts.push('');
parts.push('验证健康检查端点，确认响应状态码为 200。');
parts.push('');
parts.push('**获取和创建资源测试：**');
parts.push('');
parts.push('验证 GET 请求返回待办事项列表，验证 POST 请求创建新的待办事项。');
parts.push('');
parts.push('**404 测试：**');
parts.push('');
parts.push('验证不存在的路由返回 404。这是错误场景测试的典型示例。');
parts.push('');

// 6.4
parts.push('### 6.4 Docker 配置说明');
parts.push('');
parts.push('docker-compose.yml 文件提供了使用 Docker 运行 Bun 测试的配置。使用 oven/bun:latest 镜像，通过 volumes 将本地的 examples 目录挂载到容器中，依次运行三个测试目录的测试。');
parts.push('');
parts.push('运行方式：在 ch05-test-runner 目录下运行 docker compose up。');
parts.push('');

// 6.5
parts.push('### 6.5 项目配置建议');
parts.push('');
parts.push('对于实际项目，建议在项目根目录创建 bunfig.toml 配置文件，统一测试运行器的行为。可以配置测试文件匹配模式、超时时间、并行工作线程数量、覆盖率参数等。');
parts.push('');
parts.push('在 VS Code 中，推荐安装 Bun for VS Code 扩展，提供测试运行器的集成界面。');
parts.push('');
parts.push('在 GitHub Actions 中使用 Bun 测试时，使用 oven-sh/setup-bun 动作安装 Bun，然后运行 bun install 和 bun test --coverage。');
parts.push('');

// Summary
parts.push('---');
parts.push('');
parts.push('## 总结');
parts.push('');
parts.push('Bun 测试运行器是 Bun 运行时中最具竞争力的功能之一。它提供了与 Jest 高度兼容的 API，同时在性能上实现了显著提升。通过内置的 bun:test 模块，开发者可以在不安装任何额外依赖的情况下开始编写测试。');
parts.push('');
parts.push('本章从六个角度全面介绍了 Bun 测试运行器：使用场景方面涵盖单元测试、API 集成测试、快照测试和 DOM 测试；实现原理方面深入解析了 bun:test 模块的内部架构和 Mock 机制；潜在风险和优化方面分析了 Jest API 覆盖缺口和性能优化策略；典型问题处理方面提供了常见问题的解决方案；必备知识和技能方面介绍了测试金字塔、Mock/Stub/Spy 的区别和 TDD 方法论；示例代码方面提供了三个完整的测试示例。');
parts.push('');
parts.push('通过本章的学习，读者应该能够熟练使用 Bun 测试运行器编写高质量的测试，并能够处理测试过程中遇到的常见问题。');
parts.push('');

const content = parts.join('\n');
fs.writeFileSync(path, content, 'utf8');
console.log('Done. Total characters: ' + content.length);

// Count CJK characters
const cjkRegex = /[一-鿿㐀-䶿豈-﫿]/g;
const cjkMatches = content.match(cjkRegex);
console.log('CJK (Chinese) characters: ' + (cjkMatches ? cjkMatches.length : 0));
