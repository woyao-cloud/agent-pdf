# 第2章 复杂度分析

## 2.1 时间复杂度

**时间复杂度**（Time Complexity）是定量描述算法运行时间的函数，表示算法执行时间与输入规模增长的关系。

### 大O表示法

大O表示法（Big O Notation）描述算法执行时间的上界，即最坏情况下的时间复杂度。

| 复杂度级别 | 名称 | 典型场景 |
|-----------|------|----------|
| O(1) | 常数时间 | 数组随机访问、哈希查找 |
| O(log n) | 对数时间 | 二分查找、平衡树操作 |
| O(n) | 线性时间 | 遍历数组、链表 |
| O(n log n) | 线性对数时间 | 归并排序、快速排序 |
| O(n²) | 平方时间 | 冒泡排序、双层循环 |
| O(2ⁿ) | 指数时间 | 递归求斐波那契（未优化） |
| O(n!) | 阶乘时间 | 全排列 |

### 常见时间复杂度对比

```
n=10 时：
O(1)        █ 1次
O(log n)    ███ 3次
O(n)        ██████████ 10次
O(n log n)  ███████████ 30次
O(n²)       █████████████████████ 100次

n=100 时：
O(1)        █ 1次
O(log n)    ███ 7次
O(n)        ████████████████████ 100次
O(n log n)  ██████████████████████████████ 700次
O(n²)       ██████████████████████████████████████████████ 10000次
```

### 时间复杂度计算规则

1. **加法法则**：并列的代码段，取复杂度最大的
   ```java
   // O(n) + O(n²) = O(n²)
   for (int i = 0; i < n; i++) { }        // O(n)
   for (int i = 0; i < n; i++) {
       for (int j = 0; j < n; j++) { }   // O(n²)
   }
   ```

2. **乘法法则**：嵌套的代码段，取复杂度相乘
   ```java
   // O(n) * O(n) = O(n²)
   for (int i = 0; i < n; i++) {
       for (int j = 0; j < n; j++) { }   // O(n²)
   }
   ```

3. **忽略常数**：只保留最高阶项
   ```java
   // 2n + 3 → O(n)
   for (int i = 0; i < 2 * n; i++) { }   // O(n)
   ```

### 常见代码的时间复杂度分析

```java
// O(1) - 常数时间
public int getFirst(int[] arr) {
    return arr[0];  // 直接访问，无需循环
}

// O(n) - 线性时间
public int sum(int[] arr) {
    int sum = 0;
    for (int num : arr) {  // 遍历一次
        sum += num;
    }
    return sum;
}

// O(log n) - 对数时间（二分查找）
public int binarySearch(int[] arr, int target) {
    int left = 0, right = arr.length - 1;
    while (left <= right) {
        int mid = left + (right - left) / 2;
        if (arr[mid] == target) return mid;
        else if (arr[mid] < target) left = mid + 1;
        else right = mid - 1;
    }
    return -1;
}

// O(n log n) - 线性对数时间（归并排序的合并阶段）
public void mergeSort(int[] arr, int left, int right) {
    if (left < right) {
        int mid = left + (right - left) / 2;
        mergeSort(arr, left, mid);      // O(log n)
        mergeSort(arr, mid + 1, right); // O(log n)
        merge(arr, left, mid, right);   // O(n)
    }
}

// O(2ⁿ) - 指数时间（递归斐波那契）
public int fib(int n) {
    if (n <= 1) return n;
    return fib(n - 1) + fib(n - 2);  // 指数级递归调用
}
```

## 2.2 空间复杂度

**空间复杂度**（Space Complexity）表示算法所需内存空间与输入规模的关系。

### 常见空间复杂度

| 复杂度 | 名称 | 示例 |
|--------|------|------|
| O(1) | 常数空间 | 变量、递归调用栈 |
| O(n) | 线性空间 | 数组、集合存储 |
| O(n²) | 平方空间 | 二维数组 |

### 空间复杂度分析示例

```java
// O(1) - 只使用常数个变量
public int sum(int[] arr) {
    int sum = 0;  // 1个变量
    for (int num : arr) {
        sum += num;
    }
    return sum;
}

// O(n) - 创建一个新数组
public int[] copy(int[] arr) {
    int[] copy = new int[arr.length];  // 与输入规模相同
    for (int i = 0; i < arr.length; i++) {
        copy[i] = arr[i];
    }
    return copy;
}

// O(n) - 递归调用栈空间
public int factorial(int n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);  // 递归深度为n
}

// O(log n) - 递归调用栈（二分查找递归版）
public int binarySearch(int[] arr, int target, int left, int right) {
    if (left > right) return -1;
    int mid = left + (right - left) / 2;
    if (arr[mid] == target) return mid;
    else if (arr[mid] < target)
        return binarySearch(arr, target, mid + 1, right);
    else
        return binarySearch(arr, target, left, mid - 1);
}
```

### 原地算法 vs 非原地算法

- **原地算法（In-place）**：O(1)额外空间
- **非原地算法（Out-of-place）**：需要额外空间

```java
// 原地算法 - O(1)额外空间
public void reverse(int[] arr) {
    int left = 0, right = arr.length - 1;
    while (left < right) {
        int temp = arr[left];
        arr[left++] = arr[right];
        arr[right--] = temp;
    }
}

// 非原地算法 - O(n)额外空间
public int[] reverse(int[] arr) {
    int[] result = new int[arr.length];
    for (int i = 0; i < arr.length; i++) {
        result[i] = arr[arr.length - 1 - i];
    }
    return result;
}
```

## 2.3 递归算法分析

递归算法的复杂度分析需要解**递归方程**，常用方法有：

### 主定理（Master Theorem）

对于递归方程：`T(n) = aT(n/b) + f(n)`
- a：子问题数量
- n/b：子问题规模
- f(n)：分解和合并的时间

| 情况 | 结果 | 适用场景 |
|------|------|----------|
| f(n) = O(n<sup>log<sub>b</sub>a-ε</sup>) | T(n) = Θ(n<sup>log<sub>b</sub>a</sup>) | 子问题多于合并工作 |
| f(n) = Θ(n<sup>log<sub>b</sub>a</sup>) | T(n) = Θ(n<sup>log<sub>b</sub>a</sup> log n) | 平衡情况 |
| f(n) = Ω(n<sup>log<sub>b</sub>a+ε</sup>) | T(n) = Θ(f(n)) | 合并工作多于子问题 |

### 递归树分析

以归并排序为例：`T(n) = 2T(n/2) + O(n)`

```
T(n)
├── T(n/2) + T(n/2) + O(n)
│   ├── T(n/4) + T(n/4) + O(n/2)
│   │   └── ...
│   └── T(n/4) + T(n/4) + O(n/2)
│       └── ...
```

- 树的高度：log₂n
- 每层总工作量：O(n)
- 总时间：O(n log n)

### 递归算法空间复杂度

递归调用会占用**调用栈空间**：

```java
// 调用栈深度 = n，空间复杂度 = O(n)
public int sum(int n) {
    if (n <= 0) return 0;
    return n + sum(n - 1);
}

// 尾递归优化（某些编译器）可变为O(1)
public int sumTail(int n, int acc) {
    if (n <= 0) return acc;
    return sumTail(n - 1, acc + n);
}
```

## 2.4 摊销分析

**摊销分析**（Amortized Analysis）计算一系列操作的整体成本，再除以操作数量，得到平均每次操作的成本。

### 三种常用方法

1. **聚合分析**：整体考虑n次操作，总成本为T(n)，则每次摊销成本为T(n)/n
2. **会计方法**：为不同操作分配不同"费用"，预付储蓄来支付昂贵操作
3. **势能方法**：用势能函数跟踪数据结构状态变化

### 经典案例：动态数组扩容

```java
public class DynamicArray<E> {
    private Object[] elementData;
    private int size;
    private static final int DEFAULT_CAPACITY = 10;

    public DynamicArray() {
        this.elementData = new Object[DEFAULT_CAPACITY];
    }

    public void add(E e) {
        if (size == elementData.length) {
            grow();  // 扩容操作
        }
        elementData[size++] = e;
    }

    private void grow() {
        int newCapacity = elementData.length * 2;
        elementData = Arrays.copyOf(elementData, newCapacity);
    }
}
```

**分析**：
- 添加n个元素
- 扩容次数：1, 2, 4, 8, ..., 2^k < n
- 总复制次数：1 + 2 + 4 + ... + 2^k ≈ 2n
- 摊销成本：O(2n/n) = O(1)

### 摊销复杂度 vs 平均复杂度

| 概念 | 含义 | 例子 |
|------|------|------|
| 摊销复杂度 | n次操作的最坏情况平均 | 动态数组add()为O(1) |
| 平均复杂度 | 随机输入的期望复杂度 | 哈希查找期望O(1) |

## 2.5 实战：性能分析工具与技巧

### 代码级性能分析

```java
// 使用System.nanoTime()精确计时
public void benchmark() {
    int[] arr = new int[1000000];
    // 初始化...

    long start = System.nanoTime();
    // 待测试代码
    Arrays.sort(arr);
    long end = System.nanoTime();

    System.out.println("耗时: " + (end - start) / 1_000_000 + "ms");
}
```

### JMH微基准测试

```java
// Maven依赖: org.openjdk.jmh:jmh-core
@Benchmark
@BenchmarkMode(Mode.Throughput)
@OutputTimeUnit(TimeUnit.MILLISECONDS)
public class MyBenchmark {

    @Benchmark
    public void arrayListAdd() {
        List<Integer> list = new ArrayList<>();
        for (int i = 0; i < 1000; i++) {
            list.add(i);
        }
    }

    @Benchmark
    public void linkedListAdd() {
        List<Integer> list = new LinkedList<>();
        for (int i = 0; i < 1000; i++) {
            list.add(i);
        }
    }
}
```

### 性能分析工具

| 工具 | 用途 | 平台 |
|------|------|------|
| VisualVM | CPU/内存分析 | JDK自带 |
| JProfiler | 商业性能分析 | 跨平台 |
| async-profiler | 低开销采样 | Linux/Mac |
| YourKit | 性能分析 | 跨平台 |

### 复杂度选择建议

```
数据规模与复杂度选择：

n < 100         → O(n²)也可以接受
n < 1000        → 优先O(n log n)
n < 10000       → 必须O(n log n)或更好
n >= 10000      → 考虑O(n)或O(log n)
n >= 1000000    → 必须O(n)或O(log n)，可能需要分布式

实时系统       → 优先选择最坏情况复杂度
吞吐量优先     → 优先选择平均情况复杂度
内存受限       → 优先选择空间复杂度优的
```

## 2.6 复杂度分析练习

### 练习1：分析下列代码的时间复杂度

```java
// 代码1
for (int i = 0; i < n; i++) {
    for (int j = i; j < n; j++) {
        System.out.println(i + j);
    }
}

// 答案：O(n²) - 双重循环，j从i开始

// 代码2
int i = 1;
while (i < n) {
    i = i * 2;
}

// 答案：O(log n) - i每次翻倍

// 代码3
for (int i = 0; i < n; i++) {
    for (int j = 0; j < m; j++) {
        // O(1)操作
    }
}

// 答案：O(n * m)，如果m与n相关需具体分析

// 代码4
public void recursion(int n) {
    if (n <= 0) return;
    recursion(n - 1);
    recursion(n - 1);
}

// 答案：O(2ⁿ) - 指数级，每层两分叉
```

### 练习2：选择合适的数据结构

| 场景 | 推荐复杂度 | 推荐结构 |
|------|-----------|----------|
| 10亿数据中查找 | O(log n) | B+树/跳表 |
| 实时数据流去重 | O(1) | 哈希表/布隆过滤器 |
| 查找第K大元素 | O(1)或O(log n) | 堆 |
| 有序数据查找 | O(log n) | 平衡树/二分查找 |
| 频繁头尾操作 | O(1) | 链表/双端队列 |

## 2.7 本章小结

本章我们介绍了：
- **时间复杂度**：大O表示法、常见复杂度等级、计算规则
- **空间复杂度**：原地vs非原地算法、递归调用栈
- **递归算法分析**：主定理、递归树分析
- **摊销分析**：聚合分析、会计方法、势能方法
- **性能分析工具**：JMH、VisualVM等
- **实战技巧**：复杂度选择建议、练习题

**核心要点**：
1. 分析复杂度时先找核心操作，确定循环/递归模式
2. 关注最坏情况，但也考虑平均和摊销情况
3. 空间复杂度容易被忽视，要注意递归调用栈
4. 实际选择时考虑数据规模、实时性要求、内存限制

下一章我们将深入学习第一个数据结构：**数组与字符串**。