# 第4章 链表
## 4.1 链表类型与原理
### 解决的问题
链表（Linked List）是一种线性数据结构，通过节点间的指针链接来存储数据。与数组不同，链表不需要连续的内存空间，解决了数组插入删除性能差的问题。
### 实现原理
**单链表**：每个节点包含数据和指向下一个节点的指针
```java
class ListNode {
    int val;
    ListNode next;
    ListNode(int val) { this.val = val; }
}
```
**双向链表**：每个节点包含指向前后两个节点的指针
**循环链表**：尾节点的next指向头节点
### 核心操作复杂度
| 操作 | 单链表 | 双链表 |
|------|--------|--------|
| 头部插入/删除 | O(1) | O(1) |
| 尾部插入/删除 | O(n) | O(1) |
| 指定位置操作 | O(n) | O(n) |
| 随机访问 | O(n) | O(n) |
### 手写实现
```java
/** 单链表实现 */
public class LinkedList<E> {
    private class Node {
        E data;
        Node next;
        Node(E data) { this.data = data; }
    }

    private Node head;
    private int size;

    // 头部插入
    public void addFirst(E e) {
        Node newNode = new Node(e);
        newNode.next = head;
        head = newNode;
        size++;
    }

    // 尾部插入
    public void addLast(E e) {
        Node newNode = new Node(e);
        if (head == null) {
            head = newNode;
        } else {
            Node cur = head;
            while (cur.next != null) cur = cur.next;
            cur.next = newNode;
        }
        size++;
    }

    // 删除第一个节点
    public E removeFirst() {
        if (head == null) throw new NoSuchElementException();
        E data = head.data;
        head = head.next;
        size--;
        return data;
    }

    // 获取第index个节点
    public Node get(int index) {
        if (index < 0 || index >= size) throw new IndexOutOfBoundsException();
        Node cur = head;
        for (int i = 0; i < index; i++) cur = cur.next;
        return cur;
    }
}
```
### JDK源码解析
```java
// LinkedList核心（双向链表）
public class LinkedList<E> extends AbstractSequentialList<E> {
    // 头尾节点
    transient Node<E> first;
    transient Node<E> last;

    private static class Node<E> {
        E item;
        Node<E> next;
        Node<E> prev;
    }

    // 头部插入 - O(1)
    private void linkFirst(E e) {
        final Node<E> f = first;
        final Node<E> newNode = new Node<>(null, e, f);
        first = newNode;
        if (f == null) last = newNode;
        else f.prev = newNode;
        size++;
        modCount++;
    }

    // 尾部插入 - O(1)
    void linkLast(E e) {
        final Node<E> l = last;
        final Node<E> newNode = new Node<>(l, e, null);
        last = newNode;
        if (l == null) first = newNode;
        else l.next = newNode;
        size++;
        modCount++;
    }
}
```
## 4.2 使用场景
| 场景 | 推荐 | 原因 |
|------|------|------|
| 频繁头尾操作 | ✅ 最佳 | O(1)时间复杂度 |
| 未知大小数据 | ✅ 最佳 | 无需预分配 |
| 频繁插入删除 | ✅ 优于数组 | 无需移动元素 |
| 随机访问频繁 | ❌ 不适合 | O(n)遍历代价高 |
| 需要实现栈/队列 | ✅ 理想 | 头尾操作方便 |
## 4.3 潜在风险
| 风险 | 说明 | 解决方案 |
|------|------|----------|
| 内存不连续 | CPU缓存命中低 | 数据量小可用，量大考虑数组 |
| 遍历开销 | 每次都要从头开始 | 结合场景选择合适结构 |
| 指针丢失 | 操作不当导致断链 | 画图分析，保存必要引用 |
| 内存泄漏 | 循环引用无法GC | 手动断开引用 |
| 并发问题 | 非线程安全 | 使用ConcurrentLinkedQueue |
## 4.4 典型问题
### 反转链表
```java
// 迭代方式反转
public ListNode reverseList(ListNode head) {
    ListNode prev = null, cur = head;
    while (cur != null) {
        ListNode next = cur.next;
        cur.next = prev;
        prev = cur;
        cur = next;
    }
    return prev;
}

// 递归方式反转
public ListNode reverseListRecursive(ListNode head) {
    if (head == null || head.next == null) return head;
    ListNode newHead = reverseListRecursive(head.next);
    head.next.next = head;
    head.next = null;
    return newHead;
}
```
### 合并两个有序链表
```java
public ListNode mergeTwoLists(ListNode l1, ListNode l2) {
    ListNode dummy = new ListNode(0), cur = dummy;
    while (l1 != null && l2 != null) {
        if (l1.val <= l2.val) {
            cur.next = l1;
            l1 = l1.next;
        } else {
            cur.next = l2;
            l2 = l2.next;
        }
        cur = cur.next;
    }
    cur.next = l1 != null ? l1 : l2;
    return dummy.next;
}
```
### 检测链表是否有环
```java
public boolean hasCycle(ListNode head) {
    ListNode slow = head, fast = head;
    while (fast != null && fast.next != null) {
        slow = slow.next;
        fast = fast.next.next;
        if (slow == fast) return true;
    }
    return false;
}
```
## 4.5 本章小结
- 链表解决数组插入删除O(n)问题，头尾操作O(1)
- 双向链表支持更丰富的操作
- 适用于频繁修改、随机访问少的场景
- 注意内存连续性问题和大数据量场景