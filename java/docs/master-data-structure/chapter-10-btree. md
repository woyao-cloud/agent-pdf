# 第10章 B树与B+树
## 10.1 B树原理
### 解决的问题
B树（B-tree）是一种**自平衡的多路搜索树**，主要解决二叉树在数据量大时树高过高、IO次数多的问题。适用于**磁盘存储**场景。
### B树性质
- 每个节点最多有m个子节点
- 每个非根节点至少有m/2个子节点
- 根节点至少有2个子节点（除非是叶子）
- 有k个子节点的非叶子节点有k-1个键
- 所有叶子节点在同一层
### B树节点结构
```java
class BTreeNode {
    int[] keys;           // 键数组（有序）
    BTreeNode[] children; // 子节点数组
    int n;                // 当前键的数量
    boolean leaf;         // 是否为叶子节点
}```### B树插入
1. 找到合适叶子节点
2. 插入键，保持有序
3. 如果节点满，分裂向上传递
## 10.2 B+树原理
### B+树 vs B树
| 特性 | B树 | B+树 |
|------|-----|------|
| 数据存储 | 键和值都在节点 | 值只在叶子节点 |
| 叶子节点 | 不相连 | 链表相连 |
| 查询稳定性 | 最好O(1)，最坏O(log n) | 总是O(log n) |
| 范围查询 | 需要遍历树 | 叶子链表遍历 |
| 空间利用率 | 键分散 | 键密集在叶子 |
### B+树特点
- 所有数据都存储在叶子节点
- 叶子节点包含所有键和卫星数据
- 叶子节点间通过指针连接（适合范围查询）
- 内部节点只存储键（能容纳更多键，树更矮）
## 10.3 数据库索引为什么用B+树
### 磁盘IO特性
- 磁盘读写以页（4KB-16KB）为单位
- 每次IO只能读取一个节点
- 树高决定IO次数
### 计算示例
假设：
- 键大小：8字节
- 指针：8字节
- 节点大小：16KB
- 指针数 m ≈ 1000
- 1000^3 = 10亿条记录，3次IO即可查到！### 对比其他结构
| 结构 | 树高 | 1000万数据IO次数 |
|------|------|-----------------|
| 二叉树 | ~24 | 24次 |
| B树 | ~10 | 10次 |
| B+树 | ~3 | 3次 |
## 10.4 使用场景
| 场景 | 使用 | 原因 |
|------|------|------|
| 数据库索引 | B+树 | 减少磁盘IO，支持范围查询 |
| 文件系统 | B+树 | 目录结构组织 |
| 键值存储 | B树/LSM树 | 写入友好 |
## 10.5 潜在风险
| 风险 | 说明 | 解决方案 |
|------|------|----------|
| 写入放大 | 频繁分裂合并 | 使用LSM树等写优化结构 |
| 页分裂 | 随机写入导致性能下降 | 预热+批量写入 |
| 主键选择 | 主键过长影响性能 | 使用自增ID |
## 10.6 本章小结
- B树是多路平衡树，适合磁盘存储
- B+树在叶子节点存储所有数据，通过链表连接
- B+树是数据库索引的标准实现
- 理解B+树对数据库性能优化至关重要