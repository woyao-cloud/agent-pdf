# 第13章 并查集（Union-Find）
## 13.1 并查集原理
### 解决的问题
并查集（Union-Find/Disjoint Set）是一种**集合划分**的数据结构，解决动态连通性问题。解决的问题：- 朋友圈问题（社交网络）
- 岛屿数量（网格连通）
- 动态分组
- 最小生成树（Kruskal算法）
### 核心操作- **find(x)**：找到x所属集合的代表元素
- **union(x, y)**：合并x和y所在的集合
- **connected(x, y)**：判断x和y是否连通
### 并查集实现
```java
public class UnionFind {
    // 父节点数组，parent[i]表示i的父节点
    private int[] parent;
    // 树的大小（按秩合并优化）
    private int[] size;

    public UnionFind(int n) {
        parent = new int[n];
        size = new int[n];
        for (int i = 0; i < n; i++) {
            parent[i] = i;
            size[i] = 1;
        }
    }

    // 查找（路径压缩）
    public int find(int x) {
        if (parent[x] != x) {
            parent[x] = find(parent[x]); // 路径压缩
        }
        return parent[x];
    }

    // 合并（按秩合并）
    public void union(int x, int y) {
        int rootX = find(x);
        int rootY = find(y);
        if (rootX != rootY) {
            // 小树挂到大树上
            if (size[rootX] < size[rootY]) {
                parent[rootX] = rootY;
                size[rootY] += size[rootX];
            } else {
                parent[rootY] = rootX;
                size[rootX] += size[rootY];
            }
        }
    }

    public boolean connected(int x, int y) {
        return find(x) == find(y);
    }
}```## 13.2 优化：路径压缩+按秩合并
### 两种优化方式
| 优化 | 原理 | 效果 | |------|------|------|
| 路径压缩 | 查找时将路径上节点直接指向根 | 接近O(1) |
| 按秩合并 | 小树挂大树上 | 减少树高 |
### 时间复杂度
- 未优化：O(n)
- 路径压缩：O(α(n)) ≈ O(1)
- 完全优化：O(α(n))（α为反阿克曼函数）
## 13.3 典型问题
### 朋友圈数量
```java
public int findCircleNum(int[][] M) {  // M为关系矩阵
    int n = M.length;
    UnionFind uf = new UnionFind(n);
    for (int i = 0; i < n; i++) {
        for (int j = i + 1; j < n; j++) {
            if (M[i][j] == 1) uf.union(i, j);  // 认识则合并
        }
    }
    return (int) IntStream.range(0, n)
        .filter(i -> uf.find(i) == i).count();
}```### 岛屿数量（网格问题）
```java
class Solution {
    UnionFind uf;
    int[] dx = {-1, 1, 0, 0}, dy = {0, 0, -1, 1};

    public int numIslands(char[][] grid) {
        if (grid.length == 0) return 0;
        int m = grid.length, n = grid[0].length;
        uf = new UnionFind(m * n);
        int count = 0;

        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                if (grid[i][j] == '1') {
                    count++;
                    int idx = i * n + j;
                    for (int d = 0; d < 4; d++) {
                        int ni = i + dx[d], nj = j + dy[d];
                        if (ni >= 0 && ni < m && nj >= 0 && nj < n
                            && grid[ni][nj] == '1') {
                            int nIdx = ni * n + nj;
                            if (uf.find(idx) != uf.find(nIdx)) {
                                uf.union(idx, nIdx);
                                count--;
                            }
                        }
                    }
                }
            }
        }
        return count;
    }
}```## 13.4 使用场景
| 场景 | 说明 | |------|------| | 社交网络 | 共同好友、朋友圈 | | 图像处理 | 连通区域标记 | | 网络连接 | 网络节点连通性 | | Kruskal算法 | 最小生成树 | | 动态聚类 | 实时分组 |## 13.5 潜在风险
| 风险 | 说明 | 解决方案 |
|------|------|----------|
| 数组越界 | 索引计算错误 | 边界检查 | | 递归栈溢出 | 路径压缩递归深度大 | 迭代实现 | | 合并冲突 | 并发union可能冲突 | 加锁或使用并发版 |
## 13.6 本章小结
- 并查集是处理动态连通性问题的利器，核心操作接近O(1)- 路径压缩+按秩合并是标准优化
- 典型应用：朋友圈、岛屿数量、最小生成树