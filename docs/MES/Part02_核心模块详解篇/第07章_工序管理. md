# 第7章 工序管理
>
> 本章讲解MES系统中工序管理的核心功能，涵盖工序定义、工艺路线、工序转移与报工等关键内容
>
## 7.1 工序基础数据
>
### 7.1.1 工序定义与编码
>
工序是生产过程中的基本执行单元。每个工序需要定义清晰的结构化数据：>
```python
class Operation:    def __init__(self):        self.operation_code = ""      # 工序编号
        self.operation_name = ""    # 工序名称        self.description = ""        # 工序描述
        self.work_center = ""       # 所属工作中心
        self.standard_time = 0      # 标准工时（分钟）
        self.setup_time = 0         # 准备时间（分钟）        self.quality_rate = 1.0     # 目标合格率
```
>
工序编码通常采用分段式结构，例如：OP-001-001，其中第一段表示工段，第二段表示工序序号。>
### 7.1.2 工艺参数配置
>
每个工序可能包含多个关键工艺参数：>
```python
class ProcessParameter:    def __init__(self):        self.parameter_code = ""    # 参数编号
        self.parameter_name = ""  # 参数名称        self.unit = ""              # 单位        self.lower_limit = 0.0     # 下限
        self.target_value = 0.0   # 目标值
        self.upper_limit = 0.0    # 上限        self.data_type = ""         # 数据类型（整数/浮点数/枚举）        self.collection_type = ""  # 采集方式（自动/手动）
```
>
### 7.1.3 标准工时设定
>
标准工时是生产计划的基础，由以下部分组成：>
- **准备时间**：换型、准备物料、图纸准备等> - **加工时间**：实际生产作业时间
- **传送时间**：物料在工序间的移动时间>
>
## 7.2 工艺路线管理
>
### 7.2.1 工艺路线定义
>
工艺路线定义了产品从原材料到成品的加工路径：>
```python
class Routing:    def __init__(self):        self.routing_code = ""      # 工艺路线编号
        self.product_id = ""       # 对应产品        self.version = ""          # 版本号
        self.operations = []       # 工序列表        self.effective_date = None # 生效日期
        self.status = "draft"      # 状态：draft/effective/obsolete
```>
### 7.2.2 替代工艺路线
>
企业可能为同一产品准备多条工艺路线，用于应对不同场景：>
- 标准工艺路线：默认使用的路线- 备用工艺路线：设备故障时的替代路线
- 紧急工艺路线：产能紧张时的简化路线>
### 7.2.3 工艺版本管理
>
工艺路线需要版本管理，确保变更可追溯：>
```python
class RoutingVersion:    def __init__(self):        self.version_no = ""       # 版本号
        self.version_date = None  # 版本日期
        self.change_reason = ""   # 变更原因        self.approver = ""         # 审批人
        self.operations = []      # 该版本的工序配置
```
>
## 7.3 工序转移与报工
>
### 7.3.1 工序转移类型
>
**正推法（Forward Scheduling）**：>
按照工艺顺序从前向后推动生产。工件在完成前一道工序后，自动流转到下一道工序。>
```python
def forward_transfer(work_order, current_operation):    """正推法工序转移"""
    if current_operation.is_completed:        next_operation = get_next_operation(work_order, current_operation)
        if next_operation:            next_operation.status = "waiting"
            work_order.current_operation = next_operation            return next_operation    return None```
>
**倒推法（Backward Scheduling）**：>
从交货期倒推，确定每道工序的安排时间。适用于紧急订单，确保按时完成。>
### 7.3.2 报工数据采集
>
报工是记录生产实绩的核心功能：>```python
class WorkReport:    def __init__(self):        self.report_id = ""         # 报工单号
        self.work_order_id = ""    # 工单号        self.operation_id = ""     # 工序编号
        self.worker_id = ""        # 操作人员
        self.start_time = None     # 开始时间        self.end_time = None       # 结束时间
        self.input_quantity = 0    # 投入数量        self.output_quantity = 0   # 产出数量
        self.qualified_quantity = 0 # 合格数量        self.defect_quantity = 0   # 不良数量
```>
### 7.3.3 报工校验逻辑
>
报工时需要执行多种校验：>
- 数量平衡：产出 ≤ 投入
- 工序顺序：必须在上一道工序完成后才能报工- 时间逻辑：报工时间不能早于上道工序完成时间- 资源可用性：设备、工装必须可用
```python
def validate_report(work_report, work_order):    """报工校验"""    errors = []    # 数量平衡校验    if work_report.output_quantity > work_report.input_quantity:        errors.append("产出数量不能大于投入数量")        # 工序顺序校验    if not is_previous_completed(work_order, work_report.operation_id):        errors.append("上一道工序尚未完成")        # 时间逻辑校验    if work_report.start_time < work_order.start_time:        errors.append("报工时间不能早于工单开始时间")        return len(errors) == 0, errors
```>
>
## 7.4 工序并行与同步控制
>
### 7.4.1 工序并行条件
>
某些产品允许某些工序并行加工：>
- 同一产品不同部件可同时加工- 物料准备与加工可并行
- 检测可与加工并行>
### 7.4.2 同步点控制
>
当多道工序汇合到一点时，需要同步控制：>
- 等待所有前道工序完成- 物料齐套检查- 同步后统一下达
```python
class SyncPoint:    def __init__(self):        self.sync_point_id = ""    # 同步点编号        self.predecessors = []      # 前置工序列表
        self.is_ready = False      # 是否就绪        self.trigger_condition = "" # 触发条件（all/any/count）    def check_ready(self):        """检查同步点是否就绪"""        if self.trigger_condition == "all":            return all(p.is_completed for p in self.predecessors)
        elif self.trigger_condition == "any":            return any(p.is_completed for p in self.predecessors)        return False
```>
### 7.4.3 阻塞与等待处理
>
工序可能因以下原因阻塞：>
- 物料等待：原材料或上道工序产出未到位
- 设备等待：加工设备被其他任务占用- 人力等待：操作人员不可用- 工具等待：专用工装/模具未就绪
>
## 本章小结
>
本章详细讲解了MES系统中工序管理的核心功能。工序管理连接了工艺设计与车间执行，是生产过程控制的基础。通过本章的学习，读者应掌握工序定义、工艺路线管理、报工处理、工序同步控制等关键技能，能够构建符合企业需求的工序管理体系。