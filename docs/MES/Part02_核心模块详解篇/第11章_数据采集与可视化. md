# 第11章 数据采集与可视化
> 本章讲解MES系统中数据采集与可视化的核心技术，包括采集方式、实时处理、可视化设计
## 11.1 数据采集方式
### 11.1.1 手工采集
手工采集是最基本的数据采集方式，主要工具包括：- **扫码枪**：扫描条码/二维码，采集物料、工件、仓位信息- **PDA/手持终端**：支持条码扫描、RFID读取、数据录入- **人工录入**：通过MES界面手工填报数据手工采集的关键是设计便捷的采集流程，减少操作负担。
```python
class ManualCollection:    def __init__(self):        self.collector_id = ""        self.collection_type = ""  # scan/manual        self.location_id = ""    def collect(self, data):        """手工数据采集"""        # 记录采集人、采集时间、采集方式        record = {            'collector': self.collector_id,            'timestamp': datetime.now(),            'data': data,            'collection_type': self.collection_type        }
        return record```### 11.1.2 RFID采集
RFID（射频识别）技术适用于需要批量读取的场景：- 读取距离远（可达数米）- 可批量读取（一次读取多个标签）- 无需视觉接触
```python
class RFIDReader:    def __init__(self, reader_ip):        self.reader_ip = reader_ip        self.connection = None    def read_tags(self):        """读取RFID标签"""        tags = []        # 连接读写器        # 发送读取命令        # 解析返回数据        return tags    def continuous_read(self, callback, interval=1):        """持续读取"""        while True:            tags = self.read_tags()            if tags:                callback(tags)            time.sleep(interval)```### 11.1.3 传感器采集
工业传感器用于采集物理量数据：- 温度传感器：监测烘房、冰箱等温度- 压力传感器：监测气压、油压
- 振动传感器：监测设备运行状态```python
class SensorCollector:    def __init__(self, sensor_config):        self.sensors = sensor_config    def collect_analog(self, sensor_id):        """采集模拟量传感器数据"""        sensor = self.sensors.get(sensor_id)        value = self._read_sensor(sensor)        return {            'sensor_id': sensor_id,            'value': value,            'unit': sensor['unit'],            'timestamp': datetime.now()        }```### 11.1.4 系统接口采集系统接口采集通过与设备或上位系统集成获取数据：
- OPC UA接口：读取PLC、SCADA数据- MQTT订阅：接收设备发布的数据- API调用：调用其他系统的接口
## 11.2 实时数据处理### 11.2.1 数据流处理架构典型的数据流处理架构：```          数据源 → 采集层 → 消息队列 → 处理层 → 存储层 → 应用层          (设备/          (Kafka/          (Flink/       (时序DB/        (可视化/          传感器)       RabbitMQ)      Spark)         关系DB)       报表)```### 11.2.2 时序数据库应用时序数据库（Time Series Database）专为时序数据优化：- **InfluxDB**：开源时序数据库，支持SQL-like查询- **TimescaleDB**：PostgreSQL扩展，兼具关系型特性- **IoTDB**：Apache物联网时序数据库
```pythonfrom influxdb import InfluxDBClientclass TimeSeriesStorage:    def __init__(self):        self.client = InfluxDBClient(            host='localhost',            port=8086,            database='mes_data'        )    def write_points(self, measurement, tags, fields):        """写入时序数据"""        points = [            {                'measurement': measurement,                'tags': tags,                'fields': fields,                'time': datetime.utcnow()            }        ]        return self.client.write_points(points)    def query(self, query_sql):        """查询时序数据"""        return self.client.query(query_sql)```### 11.2.3 数据清洗与校验原始数据通常存在噪声和异常，需要清洗：- **异常值处理**：超过合理范围的数据- **缺失值处理**：补齐或标记- **格式转换**：统一数据格式- **数据去重**：去除重复记录```pythonclass DataCleaner:    def clean(self, raw_data, cleaning_rules):        """数据清洗"""        cleaned = raw_data.copy()        for rule in cleaning_rules:            if rule['type'] == 'range':                cleaned = self._filter_by_range(cleaned, rule)            elif rule['type'] == 'outlier':                cleaned = self._remove_outliers(cleaned, rule)            elif rule['type'] == 'duplicate':                cleaned = self._deduplicate(cleaned)        return cleaned```## 11.3 车间可视化### 11.3.1 车间大屏设计车间大屏是生产管理的可视化窗口：
- **整体布局**：生产概览、设备状态、异常告警、进度跟踪- **刷新频率**：关键指标5秒刷新，一般指标30秒刷新- **视觉设计**：颜色编码、图表结合、重点突出```javascript// 车间大屏数据接口示例{  "production": {    "today_output": 12500,    "target": 15000,    "completion_rate": 83.3,    "oee": 78.5  },  "equipment": {    "running": 45,    "idle": 5,    "fault": 2,    "maintenance": 3  },  "quality": {    "pass_rate": 98.2,    "defect_count": 28  }}```### 11.3.2 实时看板实时看板展示当前生产状态：- **生产进度看板**：订单完成进度- **设备状态看板**：设备运行/故障状态- **质量看板**：实时质量数据- **报工看板**：班组产量排名
### 11.3.3 生产报表生产报表用于数据分析和决策：- **日报**：每日生产完成情况- **周报**：周度生产分析- **月报**：月度汇总报表- **OEE报表**：设备综合效率分析## 11.4 数据分析应用### 11.4.1 OEE分析OEE（设备综合效率）分析：- **可用率**：实际运行时间 / 计划运行时间- **性能率**：实际产出 / 理论产出- **质量率**：合格品数量 / 总产量```pythondef calculate_oee(availability, performance, quality):    """计算OEE"""    return availability * performance * quality / 10000```### 11.4.2 产能分析产能分析帮助识别瓶颈：- 设备产能利用率分析- 产线平衡率分析- 产能瓶颈识别### 11.4.3 趋势分析趋势分析支持预测和预防：- 质量趋势分析- 效率趋势分析- 设备故障趋势预测## 本章小结本章详细讲解了MES系统中数据采集与可视化的核心技术。数据是MES系统的血液，可视化是数据价值呈现的窗口。通过本章学习，读者应掌握多种数据采集方式的原理和应用、实时数据处理架构的设计、以及可视化方案的实现方法。