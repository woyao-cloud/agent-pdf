# 第10章 自然语言处理与大模型

NLP 是 PyTorch 的另一大主场。Hugging Face 生态把 PyTorch 与 NLP / 大模型紧紧绑定 —— 几乎所有论文实现都是 PyTorch 优先。本章梳理从经典 NLP 任务到当前主流大模型训练 / 微调 / 推理的核心路径，并标记每一步的工程风险。

---

## 10.1 NLP 任务全景

| 任务 | 输入 → 输出 | 经典模型 | 当前主流 |
|------|------------|---------|---------|
| 文本分类 | 句子 → label | TextCNN / LSTM | BERT / RoBERTa fine-tune |
| 命名实体识别 | 句子 → token labels | BiLSTM-CRF | BERT + CRF / span-based |
| 机器翻译 | 句子 → 句子 | Transformer | LLM zero-shot / mBART / NLLB |
| 文本生成 | prompt → 文本 | GPT-2 | GPT-4 / Llama / Qwen |
| 问答 | (问题, 上下文) → 答案 | BERT span 抽取 | LLM RAG |
| 摘要 | 长文本 → 短文本 | BART / T5 | LLM |
| 检索 | 查询 → top-K 文档 | TF-IDF / DPR | bi-encoder + rerank |
| 推理 / Agent | 多轮对话 → 行为 | - | LLM + 工具调用 |

主线：**几乎所有任务的现代答案都是某种 LLM**。本章把 LLM 训练 / 微调 / 推理作为重点。

---

## 10.2 Hugging Face 生态

| 库 | 角色 |
|------|------|
| `transformers` | 模型实现库（PyTorch / TF / Flax） |
| `datasets` | 数据集加载与流式处理 |
| `tokenizers` | Rust 实现的高速分词 |
| `accelerate` | 多卡 / 混精 / FSDP 的便捷封装 |
| `peft` | LoRA / Adapter / Prefix Tuning |
| `trl` | RLHF / DPO 实现 |
| `bitsandbytes` | 8bit / 4bit 量化 |

最小加载示例：

```python
from transformers import AutoTokenizer, AutoModelForCausalLM
import torch

tok = AutoTokenizer.from_pretrained('Qwen/Qwen2.5-7B-Instruct')
model = AutoModelForCausalLM.from_pretrained(
    'Qwen/Qwen2.5-7B-Instruct',
    torch_dtype=torch.bfloat16,
    device_map='auto',
)
inputs = tok("你好，介绍一下 PyTorch", return_tensors='pt').to('cuda')
out = model.generate(**inputs, max_new_tokens=100)
print(tok.decode(out[0]))
```

---

## 10.3 Tokenizer：被低估的关键组件

### 10.3.1 子词分词

现代 LLM 都用子词（subword）分词：BPE / WordPiece / SentencePiece。

```python
tok.tokenize("PyTorch is great")
# ['PyTorch', 'Ġis', 'Ġgreat']        # GPT-2 BPE，Ġ 表示前导空格
```

### 10.3.2 padding / truncation

```python
tok(texts, padding=True, truncation=True, max_length=512, return_tensors='pt')
```

| 参数 | 含义 |
|------|------|
| `padding=True` | pad 到 batch 内最长 |
| `padding='max_length'` | pad 到 `max_length` |
| `truncation=True` | 超长截断 |
| `return_tensors='pt'` | 直接返回 PyTorch tensor |

### 10.3.3 特殊 token 与 chat template

聊天模型有自己的格式：

```python
messages = [
    {'role': 'system', 'content': '你是一个有用的助手'},
    {'role': 'user', 'content': '什么是 PyTorch？'},
]
text = tok.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)
```

输出根据模型不同会带 `<|im_start|>` `<|im_end|>` 等特殊 token。**必须用 `apply_chat_template`**，手写 prompt 极易格式错误导致输出退化。

### 10.3.4 attention_mask 与 position_ids

变长输入 padding 后，必须传 `attention_mask` 让模型知道哪些是 padding。Hugging Face tokenizer 自动返回。

position_ids 一般自动生成；只在做 packing（多个短样本拼成一个长样本）时需要手动构造。

---

## 10.4 LLM 微调：SFT 范式

### 10.4.1 数据格式

SFT（监督微调）数据通常是 (instruction, response) 或对话历史。常见格式：

```json
{"messages": [
  {"role": "user", "content": "问题..."},
  {"role": "assistant", "content": "答案..."}
]}
```

构造训练样本：

```python
def format_example(example, tokenizer, max_len=2048):
    text = tokenizer.apply_chat_template(
        example['messages'], tokenize=False, add_generation_prompt=False
    )
    enc = tokenizer(text, truncation=True, max_length=max_len, padding=False)
    enc['labels'] = enc['input_ids'].copy()      # next-token prediction
    # 屏蔽 user / system 部分的 loss（只在 assistant 上算 loss）
    enc['labels'] = mask_user_tokens(enc['input_ids'], enc['labels'], tokenizer)
    return enc
```

**核心要点**：loss 只对模型应该生成的部分（assistant）计算。把 user 部分的 label 设为 `-100`（CrossEntropyLoss 默认 ignore）。

### 10.4.2 全量微调 vs LoRA

```
全量微调：     7B 模型 ≈ 80GB+ 显存（含 optimizer）
LoRA r=8：     7B 模型 ≈ 16GB 显存（仅训 LoRA 权重）
QLoRA r=8：    7B 模型 ≈ 6-8GB 显存（4bit 量化基模型）
```

LoRA 设置：

```python
from peft import LoraConfig, get_peft_model

lora_cfg = LoraConfig(
    r=16,
    lora_alpha=32,
    target_modules=['q_proj', 'k_proj', 'v_proj', 'o_proj'],
    lora_dropout=0.05,
    bias='none',
    task_type='CAUSAL_LM',
)
model = get_peft_model(model, lora_cfg)
model.print_trainable_parameters()
# trainable: 8M / total: 7B  (~0.1%)
```

### 10.4.3 关键超参

LLM 微调相对稳定，常用：

| 超参 | 推荐值 | 说明 |
|------|------|------|
| lr | 2e-5（全量）/ 1e-4（LoRA） | 全量 << LoRA |
| warmup | 3-10% | 关键，没有可能崩 |
| epochs | 1-3 | SFT 数据多用 1，少用 3 |
| batch size | 全局 64-256 | 用梯度累积达到 |
| max_seq_length | 2048-8192 | 显存与模型支持的能力 |
| weight_decay | 0.0 | LLM 一般不加 |

### 10.4.4 SFT 训练脚本（Trainer 简化版）

```python
from transformers import Trainer, TrainingArguments

args = TrainingArguments(
    output_dir='out',
    per_device_train_batch_size=4,
    gradient_accumulation_steps=8,
    learning_rate=2e-5,
    num_train_epochs=2,
    bf16=True,
    gradient_checkpointing=True,
    warmup_ratio=0.05,
    lr_scheduler_type='cosine',
    logging_steps=10,
    save_steps=500,
    deepspeed='ds_config.json',     # 可选，多卡训练
    report_to='wandb',
)

trainer = Trainer(model=model, args=args, train_dataset=ds, tokenizer=tok)
trainer.train()
```

---

## 10.5 LLM 偏好对齐：DPO / RLHF

SFT 之后通常需要"对齐"：让模型偏好"被人类喜欢"的输出。

### 10.5.1 DPO（Direct Preference Optimization）

数据：(prompt, chosen_response, rejected_response)

```python
from trl import DPOTrainer, DPOConfig

config = DPOConfig(beta=0.1, ...)
trainer = DPOTrainer(
    model=model,
    ref_model=ref_model,    # 通常是 SFT 后的同一模型
    args=config,
    train_dataset=preference_ds,
    tokenizer=tok,
)
trainer.train()
```

DPO 比 PPO（经典 RLHF）简单稳定，已成为 2024 年主流。

### 10.5.2 PPO（仅了解）

PPO 流程更复杂：reward model + critic + actor + KL 约束。trl 也提供，但工程难度大、超参敏感。普通项目优先 DPO。

---

## 10.6 LLM 推理优化

直接用 `model.generate()` 在 PyTorch 中只适合 demo。**生产部署不要用**。原因：

- 没有 KV cache 复用（多轮对话浪费）
- 没有连续 batching（请求短的等长的）
- 显存 fragmentation
- attention 没用 PagedAttention

### 10.6.1 主流推理引擎

| 引擎 | 特点 | 适用 |
|------|------|------|
| **vLLM** | PagedAttention、连续 batching | 通用，社区最活跃 |
| **TensorRT-LLM** | NVIDIA 极致优化 | 单一硬件、追求 QPS |
| **TGI**（HF） | 与 transformers 紧密集成 | 易上手 |
| **lmdeploy**（上海 AI Lab） | 国产模型支持好 | Qwen / InternLM |
| **llama.cpp** | CPU/Mac 推理 | 端侧、低成本 |

vLLM 启动示例：

```bash
python -m vllm.entrypoints.openai.api_server \
    --model Qwen/Qwen2.5-7B-Instruct \
    --tensor-parallel-size 2 \
    --gpu-memory-utilization 0.9
```

### 10.6.2 量化

| 方法 | 模型大小 | 速度 | 精度损失 |
|------|--------|------|---------|
| FP16/BF16 | 100% | 1x | 极小 |
| INT8 (动态) | 50% | 1.5x | 小 |
| INT4 (GPTQ/AWQ) | 25% | 2-3x | 1-3% |
| INT4 (BitsAndBytes) | 25% | 1.5x | 中等 |

LLM 推理的事实标准是 **INT4 GPTQ 或 AWQ**：

```python
from transformers import AutoModelForCausalLM, AutoTokenizer
model = AutoModelForCausalLM.from_pretrained(
    'TheBloke/Qwen2.5-7B-Instruct-GPTQ',
    device_map='auto',
)
```

### 10.6.3 Speculative Decoding

用一个小模型猜多个 token，大模型验证 —— 多数 token 接受时显著提速。已被 vLLM、TensorRT-LLM、Hugging Face transformers 支持。

---

## 10.7 RAG：检索增强生成

LLM 加上外部知识库，是当前几乎所有"AI 助手"的事实标准。

### 10.7.1 基础流水线

```
用户 query
   ↓
embedding 模型 → 向量
   ↓
向量数据库 → top-K 文档
   ↓
prompt 拼接（query + 文档）
   ↓
LLM 生成答案
```

### 10.7.2 PyTorch 中的 embedding 模型

```python
from sentence_transformers import SentenceTransformer
embedder = SentenceTransformer('BAAI/bge-large-zh-v1.5')
emb = embedder.encode(['查询文本'], normalize_embeddings=True)
```

或用 HF transformers 直接拿 [CLS] / pooling：

```python
out = bert(**inputs)
emb = out.last_hidden_state[:, 0]   # CLS
emb = F.normalize(emb, dim=-1)
```

### 10.7.3 向量库

PyTorch 不负责向量库；用 FAISS / Milvus / Chroma / Qdrant。FAISS 是最轻量：

```python
import faiss
index = faiss.IndexFlatIP(768)        # 内积 = 余弦（如果向量已归一化）
index.add(embeddings)
D, I = index.search(query_emb, k=5)
```

### 10.7.4 RAG 中的 reranker

第一阶段 bi-encoder 召回 top-100，第二阶段 cross-encoder（更精确但慢）精排：

```python
from sentence_transformers import CrossEncoder
reranker = CrossEncoder('BAAI/bge-reranker-large')
scores = reranker.predict([(query, doc) for doc in docs])
```

---

## 10.8 经典 NLP 任务（仍有用）

虽然 LLM 强大，很多生产场景仍用小模型：成本、延迟、隐私。

### 10.8.1 文本分类（BERT fine-tune）

```python
from transformers import AutoModelForSequenceClassification
model = AutoModelForSequenceClassification.from_pretrained(
    'bert-base-chinese', num_labels=10
)
```

数据 → tokenizer → DataCollator → Trainer。50 行代码达到 SOTA 级精度。

### 10.8.2 NER（token classification）

```python
from transformers import AutoModelForTokenClassification
model = AutoModelForTokenClassification.from_pretrained(
    'bert-base-chinese', num_labels=len(label_list)
)
```

特殊处理：subword 后第一个 token 用真实 label，后续 subword label 设 -100（不计入 loss）。

### 10.8.3 序列到序列（翻译 / 摘要）

T5、BART、mT5 系列。Hugging Face 提供完整 fine-tune 模板。

---

## 10.9 NLP 风险与陷阱

| 风险 | 表现 | 处理 |
|------|------|------|
| 训练 / 推理时 chat template 不一致 | 模型输出退化（忽然胡言乱语） | 永远用 `apply_chat_template` |
| 数据中包含训练答案泄露 | 评估虚高 | 严格切分 train/val |
| 特殊 token 泄露到生成 | 输出包含 `<\|im_end\|>` 等 | 设 `eos_token_id` 终止；推理后 strip |
| 梯度爆炸（长序列） | 训练 NaN | grad clipping + bf16 |
| KV cache OOM | 多轮长对话推理 OOM | vLLM PagedAttention / 限定历史 |
| RLHF reward hacking | 模型钻 reward model 漏洞 | 加 KL 约束、人工评测 |
| RAG 检索召回低 | 答非所问 | 用更好 embedding + reranker |
| 量化精度损失 | 小数学题/代码退化 | 改用更高 bit / 混合 |

---

## 10.10 NLP 项目工程模板

```
项目结构
├── data/
│   ├── prepare.py         # 拉数据 / 清洗
│   └── dataset.py
├── models/
│   └── load.py            # transformers 加载
├── training/
│   ├── sft.py             # SFT
│   ├── dpo.py             # 偏好对齐
│   └── trainer_callbacks.py
├── inference/
│   ├── server_vllm.sh
│   └── client.py
├── eval/
│   ├── benchmark.py       # MMLU / CMMLU / 自定义
│   └── human_eval.py
└── configs/
```

---

## 10.11 本章小结

- NLP 与 PyTorch 的事实主流是 Hugging Face transformers + datasets + accelerate + peft + trl
- LLM 微调首选 LoRA/QLoRA；偏好对齐首选 DPO；推理首选 vLLM / TensorRT-LLM
- tokenizer + chat template 是 LLM 训练 / 推理一致性的根；切勿手写 prompt
- RAG 是当前 AI 应用的事实架构；用 bi-encoder + 向量库 + reranker
- 量化（INT4 GPTQ/AWQ）让 7B 模型能跑在消费级 GPU
- 风险集中在数据泄露、template 不一致、KV cache OOM、reward hacking

下一章把视野拉回更经典的场景：推荐系统、时序、强化学习、生成模型。
