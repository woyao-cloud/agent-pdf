# 第13章 复杂项目架构设计
## 13.1 架构设计原则
### 13.1.1 分层架构
**经典三层架构**```mermaidgraph TBA[表示层] --> B[业务逻辑层]B --> C[数据访问层]```**各层职责**```python# 表示层 (Presentation)from flask import Flask, jsonifyapp = Flask(__name__)@app.route('/api/users')def get_users():    return jsonify(users)# 业务逻辑层 (Business Logic)class UserService:    def get_users(self):        return self. repository. find_all()    def create_user(self, data):        # 业务规则验证        if not self. validate_email(data['email']):            raise ValueError("Invalid email")        return self. repository. create(data)# 数据访问层 (Data Access)class UserRepository:    def find_all(self):        return db. query(User). all()    def create(self, data):        user = User(**data)        db. add(user)        db. commit()        return user```### 13.1.2 微服务 vs 单体
**架构选择因素**| 因素 | 单体 | 微服务 ||------|------|--------|| 团队规模 | 小团队 (<10人) | 大团队 || 发布频率 | 低 | 高 || 技术多样性 | 单一 | 多样 || 复杂度 | 低 | 高 || 扩展性 | 垂直扩展 | 水平扩展 |**微服务拆分策略**```bashclaude "为这个电商系统设计微服务拆分方案"# 输出建议：# - 用户服务：用户管理、认证# - 商品服务：商品目录、搜索# - 订单服务：订单处理# - 支付服务：支付处理# - 通知服务：消息推送```### 13.1.3 领域驱动设计
**DDD 核心概念**```领域驱动设计 (DDD)：- 实体 (Entity)：有唯一标识的对象- 值对象 (Value Object)：无标识的属性集合- 聚合根 (Aggregate Root)：实体间的一致性边界- 领域事件 (Domain Event)：领域内发生的业务事件```**示例代码**```python# 实体class Order:    def __init__(self, order_id):        self. id = order_id        self. items = []        self. status = OrderStatus. PENDING    def add_item(self, product, quantity):        if self. status != OrderStatus. PENDING:            raise ValueError("Cannot add items")        self. items. append(OrderItem(product, quantity))# 聚合根class OrderAggregate:    def __init__(self):        self. orders = {}    def place_order(self, order):        order. status = OrderStatus. CONFIRMED        self. orders[order. id] = order        # 发布领域事件        self. events. publish(OrderPlacedEvent(order))```## 13.2 设计模式应用
### 13.2.1 创建型模式
**工厂模式**```python
class LoggerFactory:    @staticmethod    def create_logger(logger_type):        loggers = {            'file': FileLogger,            'console': ConsoleLogger,            'database': DatabaseLogger        }        return loggers[logger_type]()# 使用logger = LoggerFactory. create_logger('file')```**单例模式**```python
class ConfigManager:    _instance = None    _config = None        def __new__(cls):        if cls. _instance is None:            cls. _instance = super().__new__(cls)        return cls. _instance        def load(self, config_file):        self. _config = yaml. load(open(config_file))```**建造者模式**```python
class QueryBuilder:    def __init__(self):        self. query = Query()        def select(self, *fields):        self. query. fields = fields        return self        def where(self, condition):        self. query. conditions. append(condition)        return self        def build(self):        return self. query# 使用query = (QueryBuilder()    . select('name', 'email')    . where('age > 18')    . build())```### 13.2.2 结构型模式
**适配器模式**```python
class OldAPI:    def get_user_data(self):        return {'name': 'John', 'age': 30}class NewAPI:    def fetch_user(self):        return {            'username': 'John',            'birthday': '1994-01-01'        }class UserAdapter:    def __init__(self, api):        self. api = api        def get_user(self):        if isinstance(self. api, OldAPI):            data = self. api. get_user_data()            return {'username': data['name']}        return self. api. fetch_user()```**装饰器模式**```python
def log_calls(func):    def wrapper(*args, **kwargs):        print(f"Calling {func. __name__}")        result = func(*args, **kwargs)        print(f"Finished {func. __name__}")        return result    return wrapper@log_callsdef process_data(data):    return data. upper()```### 13.2.3 行为型模式
**观察者模式**```python
class EventManager:    def __init__(self):        self. observers = {}        def subscribe(self, event_type, observer):        if event_type not in self. observers:            self. observers[event_type] = []        self. observers[event_type]. append(observer)        def notify(self, event_type, data):        for observer in self. observers. get(event_type, []):            observer. update(data)# 使用class UserObserver:    def update(self, event):        print(f"User event: {event}")event_manager. subscribe('user_created', UserObserver())```**策略模式**```python
class PaymentStrategy:    def pay(self, amount):        passclass CreditCardPayment(PaymentStrategy):    def pay(self, amount):        print(f"Paid {amount} via Credit Card")class PayPalPayment(PaymentStrategy):    def pay(self, amount):        print(f"Paid {amount} via PayPal")class ShoppingCart:    def __init__(self):        self. items = []        self. payment_strategy = None        def set_payment(self, strategy):        self. payment_strategy = strategy        def checkout(self):        total = sum(item. price for item in self. items)        self. payment_strategy. pay(total)```## 13.3 架构决策文档
### 13.3.1 ADR 撰写
**ADR 格式**```markdown# ADR-001: 使用 PostgreSQL 作为主数据库## 状态：已接受## 背景- 项目需要可靠的关系型数据库- 数据量预计 100GB- 需要支持复杂查询## 决策- 使用 PostgreSQL 15- 使用主从复制## 后果- 需要 DBA 维护- 学习曲线- 性能优化需求```**使用 Claude 生成 ADR**```bashclaude "为选择消息队列系统生成 ADR"# 输出完整的架构决策文档```### 13.3.2 权衡分析
**决策矩阵**| 方案 | 性能 | 成本 | 易用性 | 可扩展性 | 总分 ||------|------|------|--------|----------|------|| RabbitMQ | 8 | 7 | 9 | 7 | 31 || Kafka | 9 | 8 | 6 | 9 | 32 || Redis | 7 | 9 | 8 | 6 | 30 |**权衡分析示例**```markdown## 权衡分析：缓存方案选择### Redis vs Memcached| 维度 | Redis | Memcached ||------|-------|----------|| 持久化 | 支持 | 不支持 || 数据结构 | 丰富 | 简单 || 内存效率 | 中等 | 高 || 集群支持 | 官方 | 第三方 |### 决策：选择 Redis- 需要持久化功能- 需要丰富的数据结构- 社区活跃度高```### 13.3.3 架构评审
**评审清单**- [ ] 架构是否符合业务需求- [ ] 可扩展性是否满足预期- [ ] 技术选型是否合理- [ ] 安全性是否考虑充分- [ ] 性能是否能达到要求- [ ] 成本是否在预算内- [ ] 团队是否有相关经验```bashclaude "对这个架构设计进行评审"```## 本章小结本章介绍了复杂项目架构设计。涵盖分层架构、微服务 vs 单体选择、DDD 概念、设计模式应用和架构决策文档。## 练习题1. 使用 DDD 重新设计一个现有模块2. 为技术选型生成 ADR3. 使用设计模式重构代码